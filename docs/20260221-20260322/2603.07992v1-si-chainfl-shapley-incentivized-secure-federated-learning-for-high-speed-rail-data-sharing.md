---
title: "SI-ChainFL: Shapley-Incentivized Secure Federated Learning for High-Speed Rail Data Sharing"
title_zh: SI-ChainFL：面向高铁数据共享的 Shapley 激励安全联邦学习
authors: "Mingjie Zhao, Cheng Dai, Fei Chen, Xin Chen, Kaoru Ota, Mianxiong Dong, Bing Guo"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07992v1"
tags: ["query:urt-pf"]
score: 9.0
evidence: 基于联邦学习的高铁客流预测
tldr: 针对高铁数据共享中联邦学习存在的激励不足和单点故障问题，本文提出SI-ChainFL框架。该框架利用Shapley值综合考虑稀有事件效用、数据多样性、质量和及时性来量化客户端贡献，并结合聚类策略降低计算开销。同时，设计了基于区块链的共识协议实现去中心化聚合，将聚合权限与激励挂钩。实验证明，该方法在恶意攻击下表现稳健，显著提升了模型精度。
selection_source: fresh_fetch
motivation: 旨在解决高铁跨部门流量预测中联邦学习面临的搭便车、模型投毒及中心化聚合带来的单点故障风险。
method: 提出基于Shapley值的贡献度量化指标，结合客户端聚类加速评估，并利用区块链共识协议实现与激励挂钩的去中心化聚合。
result: "在多种数据集及高铁流量数据上，SI-ChainFL在面对90%恶意客户端攻击时仍能保持有效，准确率比RAGA高出14.12%。"
conclusion: SI-ChainFL通过结合贡献感知激励与去中心化聚合，为高铁等关键基础设施的安全高效数据共享提供了可靠且具备理论保障的方案。
---

## 摘要
在高速铁路（HSR）系统中，联邦学习（FL）能够在不共享原始数据的情况下实现跨部门的流量预测。然而，现有方案存在两个关键局限性：（1）激励机制不足，导致搭便车行为和模型投毒；（2）中心化聚合，引入了单点故障。我们提出了一种安全高效的框架 SI-ChainFL，通过将贡献感知的激励机制与去中心化聚合相结合来解决这些问题。首先，我们使用 Shapley 值指标量化客户端贡献，该指标综合考虑了稀有事件效用、数据多样性、数据质量和及时性。为了降低计算开销，我们进一步开发了一种稀有正样本驱动的客户端聚类策略，以加速 Shapley 值的估算。此外，我们设计了一种基于区块链的去中心化聚合共识协议，其中聚合资格与 Shapley 激励挂钩。这一设计激励客户端提交高质量的更新，并实现了高效且安全的全局聚合。在 MNIST、CIFAR 10、CIFAR 100 以及高铁流量数据集上的实验表明，SI-ChainFL 在 PA 攻击下即使存在 90% 的恶意客户端仍能保持有效，其准确率比 RAGA 高出 14.12%。理论分析进一步保证了性能的上界。

## Abstract
In high-speed rail (HSR) systems, federated learning (FL) enables cross-departmental flow prediction without sharing raw data. However, existing schemes suffer from two key limitations: (1) insufficient incentives, leading to free-riding and model poisoning; and (2) centralized aggregation, which introduces a single point of failure. We propose a secure and efficient framework SI-ChainFL that addresses these issues by combining contribution-aware incentives with decentralized aggregation. First, we quantify client contributions using a Shapley value metric that jointly considers rare-event utility, data diversity, data quality, and timeliness. To reduce computational overhead, we further develop a rare positive driven client clustering strategy to accelerate Shapley estimation. Moreover, we design a blockchain-based consensus protocol for decentralized aggregation, where aggregation eligibility is tied to Shapley incentives. This design motivates clients to submit high-quality updates and enables efficient and secure global aggregation. Experiments on MNIST, CIFAR 10 and CIFAR 100, and a HSR flow dataset show that SI ChainFL remains effective under 90% malicious clients in PA attacks, achieving 14.12% higher accuracy than RAGA. Theoretical analysis further guarantees an upper bound on performance

---

## 论文详细总结（自动生成）

### SI-ChainFL：面向高铁数据共享的 Shapley 激励安全联邦学习论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
随着高铁（HSR）网络的扩张，跨部门（如车站、票务、气象）的数据共享对于客流预测和拥堵预警至关重要。然而，隐私保护法规（如 GDPR）限制了原始数据的直接交换。联邦学习（FL）虽能实现“数据不动模型动”，但在实际应用中面临两大挑战：
*   **激励机制缺失**：参与者可能通过“搭便车”（不贡献数据只获取模型）或“模型投毒”（上传恶意更新）来破坏系统。
*   **中心化风险**：传统的中心化聚合服务器存在单点故障风险，且难以保证聚合过程的透明与公正。
本文旨在构建一个既能公平量化贡献、又能抵御恶意攻击的去中心化联邦学习框架。

#### 2. 方法论：核心思想与关键技术
论文提出了 **SI-ChainFL** 框架，其核心思想是将多维度的贡献评估（基于 Shapley 值）与基于区块链的共识聚合深度耦合。
*   **多目标 Shapley 值建模**：不同于仅考虑样本量的传统方法，该指标综合了：
    *   **稀有事件效用**：针对高铁突发客流等稀有正样本的预测贡献。
    *   **数据多样性**：通过特征表示的余弦相似度衡量数据的独特价值。
    *   **数据质量与及时性**：考虑数据缺失率、离群值及随时间衰减的贡献权重。
*   **正样本驱动的近似计算**：为解决 Shapley 值计算复杂度随节点数指数级增长的问题，提出了一种聚类策略。通过识别对稀有正样本有显著影响的客户端并进行聚类，将计算复杂度从 $O(2^n)$ 降低到近线性水平。
*   **基于区块链的共识协议**：设计了一种与激励挂钩的共识机制。验证节点根据 Shapley 得分对客户端更新进行投票，只有获得足够票数的更新才能进入聚合集。聚合权重由 Shapley 值决定，且最终模型仅分发给合格的贡献者。

#### 3. 实验设计
*   **数据集**：
    *   **基准数据集**：MNIST、CIFAR-10、CIFAR-100（用于验证分类任务的收敛性）。
    *   **交通数据集**：PeMS07（高速公路传感器数据）。
    *   **真实高铁数据集（HSR）**：包含中国 108 个高铁站的到发客流及对应气象数据。
*   **对比方法（Benchmarks）**：包括经典方法（FedAvg, FedProx）、边缘加速方法（FedEdge）、图联邦学习（FedSage）、针对突发流量的方法（P2FMS）以及鲁棒聚合方法（FLTrust, RAGA）。
*   **攻击场景**：模拟了搭便车攻击（FR）和投毒攻击（PA），恶意客户端比例从 10% 设定至极端的 90%。

#### 4. 资源与算力
*   **硬件环境**：实验使用了配备 **NVIDIA RTX 5090 (32GB)** GPU 和 25 vCPU Intel Xeon Platinum 8470Q 的工作站。
*   **软件框架**：基于 PyTorch 2.8.0。
*   **训练细节**：设置了 100 轮通信，每轮本地迭代 5 次，学习率为 0.01。文中明确记录了不同数据集下的 Shapley 计算耗时。

#### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **准确性与收敛性实验**：在 4 个数据集上验证了不同迭代次数和客户端数量下的表现。
*   **鲁棒性实验**：针对 FR 和 PA 攻击，测试了不同恶意节点比例（最高 90%）下的模型精度和平均绝对误差（MAE）。
*   **效率实验**：对比了近似 Shapley 计算与传统采样方法的耗时。
*   **消融实验**：分别验证了贡献感知激励（SI）和区块链聚合（ChainFL）模块的有效性。
**评价**：实验设计非常充分，覆盖了从标准图像识别到复杂时空预测的多种场景，且极端攻击下的对比增强了结果的说服力。

#### 6. 主要结论与发现
*   **极强的鲁棒性**：在 90% 的客户端发动投毒攻击的极端情况下，SI-ChainFL 仍能保持有效，准确率比目前最先进的鲁棒聚合算法 RAGA 高出 **14.12%**。
*   **高效性**：提出的近似计算方法在 HSR 数据集上将 Shapley 计算时间缩短至传统采样方法的 **1/8**。
*   **公平性与安全性**：通过将聚合权限与 Shapley 值挂钩，成功过滤了恶意节点，并激励了高质量数据的贡献。

#### 7. 优点
*   **多维贡献量化**：特别考虑了高铁场景中的“稀有事件”（如春运高峰），使激励机制更符合实际业务需求。
*   **去中心化信任**：利用区块链消除了对中心化服务器的依赖，解决了审计和单点故障问题。
*   **理论支撑**：论文提供了详细的安全性分析，包括差分隐私（DP）的组合定理证明和恶意权重影响的上界分析。

#### 8. 不足与局限
*   **计算开销**：尽管进行了近似优化，但相比于简单的 FedAvg，计算 Shapley 值和运行区块链共识仍会带来额外的计算和通信开销。
*   **隐私与精度的权衡**：为了实现差分隐私保护，模型在某些数据集上的精度略低于无噪声版本，这在极高精度要求的场景下可能需要进一步权衡。
*   **应用限制**：虽然在高铁数据上表现优异，但其聚类策略高度依赖于“稀有正样本”的定义，在正负样本均衡的通用任务中，加速效果可能不明显。

（完）
