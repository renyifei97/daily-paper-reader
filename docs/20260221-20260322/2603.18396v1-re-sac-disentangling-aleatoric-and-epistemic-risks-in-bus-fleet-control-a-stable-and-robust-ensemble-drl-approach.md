---
title: "RE-SAC: Disentangling aleatoric and epistemic risks in bus fleet control: A stable and robust ensemble DRL approach"
title_zh: RE-SAC：解耦公交车队控制中的偶然风险与认知风险：一种稳定且鲁棒的集成深度强化学习方法
authors: "Yifan Zhang, Liang Zheng"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18396v1"
tags: ["query:urt"]
score: 9.0
evidence: 用于公交车队控制和客流需求管理的集成深度强化学习
tldr: 针对公交车辆驻留控制中随机交通和乘客需求带来的不确定性，本文提出了一种鲁棒集成软演员-评论家（RE-SAC）框架。该方法通过解耦偶然不确定性（噪声）和认知不确定性（数据不足），利用基于IPM的权重正则化和多样化Q集成，解决了标准DRL在波动环境下的Q值不稳定和策略崩溃问题，显著提升了公交调度的鲁棒性和效率。
selection_source: fresh_fetch
motivation: 传统的深度强化学习在处理公交控制时，因无法区分偶然和认知不确定性，常导致Q值估计偏差和策略失效。
method: 提出RE-SAC框架，结合基于积分概率度量（IPM）的权重正则化应对偶然风险，并利用多样化Q集成抑制认知风险。
result: "在双向公交走廊模拟中，RE-SAC的累积回报显著优于基准算法，且在稀有分布外状态下的Q值估计误差降低了高达62%。"
conclusion: 该研究证明了显式解耦两种不确定性对于提高强化学习在高度波动交通环境下的稳定性和鲁棒性至关重要。
---

## 摘要
由于随机的交通状况和乘客需求，公交驻留控制具有挑战性。尽管深度强化学习（DRL）展现出应用前景，但标准的 Actor-Critic 算法在多变的环境中面临 Q 值不稳定的问题。这种不稳定性的一个关键来源是混淆了两种截然不同的不确定性：偶然不确定性（不可约噪声）和认知不确定性（数据不足）。将这两者视为单一风险会导致在噪声状态下的价值低估，从而引发灾难性的策略崩溃。我们提出了一种鲁棒集成软 Actor-Critic（RE-SAC）框架，以显式解耦这些不确定性。RE-SAC 对 Critic 网络应用基于积分概率度量（IPM）的权重正则化以规避偶然风险，在无需昂贵的内循环扰动的情况下，为鲁棒贝尔曼算子提供平滑的解析下界。为了应对认知风险，多样化的 Q 集成对数据覆盖稀疏区域中过度自信的价值估计进行惩罚。这种双重机制防止了集成方差将噪声误认为数据缺口，这是我们在消融实验中发现的一种失效模式。在真实的双向公交走廊模拟实验中，结果表明 RE-SAC 相比于原始 SAC（约 -0.55e6）实现了最高的累积奖励（约 -0.4e6）。马氏稀有度分析证实，在稀有的分布外状态下，RE-SAC 将 Oracle Q 值估计误差降低了高达 62%（MAE 为 1647，而对比组为 4343），证明了其在高交通波动性下的卓越鲁棒性。

## Abstract
Bus holding control is challenging due to stochastic traffic and passenger demand. While deep reinforcement learning (DRL) shows promise, standard actor-critic algorithms suffer from Q-value instability in volatile environments. A key source of this instability is the conflation of two distinct uncertainties: aleatoric uncertainty (irreducible noise) and epistemic uncertainty (data insufficiency). Treating these as a single risk leads to value underestimation in noisy states, causing catastrophic policy collapse. We propose a robust ensemble soft actor-critic (RE-SAC) framework to explicitly disentangle these uncertainties. RE-SAC applies Integral Probability Metric (IPM)-based weight regularization to the critic network to hedge against aleatoric risk, providing a smooth analytical lower bound for the robust Bellman operator without expensive inner-loop perturbations. To address epistemic risk, a diversified Q-ensemble penalizes overconfident value estimates in sparsely covered regions. This dual mechanism prevents the ensemble variance from misidentifying noise as a data gap, a failure mode identified in our ablation study. Experiments in a realistic bidirectional bus corridor simulation demonstrate that RE-SAC achieves the highest cumulative reward (approx. -0.4e6) compared to vanilla SAC (-0.55e6). Mahalanobis rareness analysis confirms that RE-SAC reduces Oracle Q-value estimation error by up to 62% in rare out-of-distribution states (MAE of 1647 vs. 4343), demonstrating superior robustness under high traffic variability.

---

## 论文详细总结（自动生成）

### 论文总结：RE-SAC 解耦公交车队控制中的偶然与认知风险

#### 1. 核心问题与整体含义（研究动机和背景）
公交车队控制（如驻留控制）是城市交通中的经典难题，其核心挑战在于环境的高度**随机性**（如交通拥堵波动、乘客到达率突变）。
*   **研究动机**：现有的深度强化学习（DRL）方法（如 SAC）在处理此类高方差环境时，常出现“Q值中毒”（Q-value poisoning）现象，导致价值估计崩溃或严重低估。
*   **核心问题**：作者认为这种不稳定性源于对两种不确定性的混淆：
    1.  **偶然不确定性（Aleatoric Uncertainty）**：环境固有的、不可约的噪声（如信号灯随机等待）。
    2.  **认知不确定性（Epistemic Uncertainty）**：由于经验池数据覆盖不足导致的模型知识缺失（如罕见的极端拥堵）。
*   **整体含义**：如果将两者混为一谈，智能体会将正常的噪声误判为未知风险，从而陷入过度保守的“低估陷阱”，导致策略失效。

#### 2. 论文提出的方法论：RE-SAC 框架
核心思想是**显式解耦**并针对性地抑制这两类风险。
*   **偶然风险缓解（基于 IPM 的正则化）**：
    *   利用积分概率度量（IPM）理论，证明了对 Critic 网络权重进行 $\ell_1$ 范数正则化，等价于在 1-Wasserstein 距离定义的模型扰动下寻找鲁棒下界。
    *   这种方法无需像传统鲁棒 RL 那样进行昂贵的内循环状态扰动采样，计算效率极高。
*   **认知风险缓解（多样化 Q 集成）**：
    *   维护 $K$ 个独立的 Q 网络集成。在数据稀疏区域，不同网络预测差异会变大。
    *   通过在贝尔曼更新中减去集成方差（惩罚项），实现“面对未知时的悲观主义”，确保策略留在数据支持的“信任区”。
*   **算法流程**：
    *   在 SAC 基础上，修改 Critic 的目标值计算：$y = r + \gamma ( \text{Mean}(Q) - \lambda_{epi} \cdot \text{Var}(Q) - \lambda_{ale} \cdot \|W\|_1 )$。
    *   Actor 采用下置信界（LCB）目标进行更新，平衡探索与稳健性。
    *   引入类别嵌入（Categorical Embeddings）处理公交线路中的离散特征（如站点 ID、车辆 ID）。

#### 3. 实验设计
*   **场景**：一个高保真的双向公交走廊模拟环境，包含 22 个站点，模拟 13 小时的运营。乘客到达服从泊松分布，交通速度服从随时间变化的随机分布。
*   **Benchmark**：以车头间隔均匀性（Headway Regularity）为核心的奖励函数。
*   **对比方法**：
    1.  **Vanilla SAC**：标准软演员-评论家算法。
    2.  **DSAC**：分布深度强化学习（处理环境随机性）。
    3.  **BAC (BEE Actor-Critic)**：旨在通过利用过去成功经验防止价值低估的先进算法。
    4.  **消融实验**：仅包含认知风险（Epistemic-only）和仅包含偶然风险（Aleatoric-only）的变体。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用 PyTorch 实现，并采用了向量化 Critic 模块（VectorizedCritic）以提升多集成网络下的计算效率。超参数设置中提到 Batch Size 为 2048，经验池大小为 $10^6$。

#### 5. 实验数量与充分性
*   **实验规模**：每个算法训练 500 个回合（Episodes），评估结果基于 10 次独立运行的平均值。
*   **充分性**：实验设计较为充分且客观。除了标准的累积奖励曲线外，还进行了：
    1.  **Q 值稳定性分析**：展示了不同方法在训练过程中的价值估计波动。
    2.  **马氏稀有度分析（Mahalanobis Rareness）**：这是一种创新的评估方式，通过计算状态在特征空间中的稀有程度，验证模型在分布外（OOD）状态下的表现。
    3.  **消融实验**：清晰地展示了如果不解耦不确定性（特别是仅使用集成时），模型会发生灾难性崩溃。

#### 6. 主要结论与发现
*   **性能优越性**：RE-SAC 在累积奖励上显著优于所有基准算法（约 $-0.4 \times 10^6$ vs SAC 的 $-0.55 \times 10^6$）。
*   **解耦的必要性**：消融实验显示，“仅认知风险”模型在训练中期发生了严重的价值低估和策略崩溃，证明了在噪声环境下，必须通过正则化处理偶然风险，否则集成方差会失效。
*   **OOD 鲁棒性**：在罕见交通状态下，RE-SAC 的 Q 值估计误差比 SAC 降低了 62%，证明了其在极端情况下的可靠性。

#### 7. 优点（亮点）
*   **理论支撑扎实**：将鲁棒 MDP、IPM 理论与深度强化学习有机结合，提供了严谨的数学推导（包括收敛性证明）。
*   **计算效率**：通过权重正则化替代显式的对抗扰动采样，在保持鲁棒性的同时几乎不增加训练负担。
*   **解决实际痛点**：针对公交控制中常见的“价值崩溃”给出了直击本质的解释和解决方案。

#### 8. 不足与局限
*   **场景单一**：实验仅在单条双向走廊进行，尚未在复杂的城市级多线路网络（涉及换乘协调）中验证。
*   **超参数敏感性**：算法引入了 $\lambda_{ale}$ 和 $\lambda_{epi}$ 等多个风险系数，这些参数在不同交通系统中的通用性或自适应调节能力有待进一步研究。
*   **算力细节缺失**：未提供具体的硬件消耗数据，不便于其他研究者评估其部署成本。
*   **应用限制**：作为单智能体方法，在处理超大规模车队（数百辆车）时的扩展性可能面临挑战。

（完）
