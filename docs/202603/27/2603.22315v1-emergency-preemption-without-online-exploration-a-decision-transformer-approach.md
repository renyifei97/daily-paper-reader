---
title: "Emergency Preemption Without Online Exploration: A Decision Transformer Approach"
title_zh: 无需在线探索的应急抢占：一种 Decision Transformer 方法
authors: "Haoran Su, Hanxiao Deng, Yandong Sun"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.22315v1"
tags: ["query:urt-c"]
score: 8.0
evidence: 用于廊道优化和多智能体空间协调的决策Transformer
tldr: 针对紧急车辆（EV）响应时间优化问题，本文提出一种基于决策变换器（DT）的离线预抢占框架。该方法将交通信号优化建模为离线序列预测，无需在线环境交互，并通过目标回报实现对紧急程度的灵活控制。通过多智能体扩展（MADT）和图注意力机制，在LightSim模拟器中显著降低了EV行驶时间及平民车辆延迟，优于传统的固定配时和在线强化学习基准。
selection_source: fresh_fetch
motivation: 现有的紧急信号预抢占策略多为被动且不可控，且在线强化学习在实际部署中面临高昂的交互成本和安全性挑战。
method: 采用决策变换器进行离线序列建模，并结合图注意力机制构建多智能体框架，通过调节目标回报标量实现多目标的权衡控制。
result: "在不同规模的网格测试中，该方法将EV行驶时间最高降低了45.2%，同时实现了最低的平民车辆延迟和最少的停车次数。"
conclusion: 研究证明了离线决策变换器在紧急交通调度中的优越性，为实现高效、可控且无需在线探索的交通信号预抢占提供了新方案。
---

## 摘要
应急车辆 (EV) 的响应时间是生存结果的关键决定因素，然而目前部署的信号抢占策略仍处于被动且不可控的状态。我们提出了一种基于 Decision Transformer (DT) 的回报条件化应急走廊优化框架。通过将走廊优化建模为离线、回报条件化的序列建模，我们的方法：(1) 消除了策略学习过程中的在线环境交互；(2) 通过单一的目标回报标量实现了调度层级的紧急程度控制；(3) 通过带有图注意力机制的多智能体 Decision Transformer (MADT) 扩展至多智能体场景以实现空间协调。在 LightSim 模拟器上，DT 在 4x4 网格中相对于固定配时抢占将 EV 平均行驶时间缩短了 37.7%（88.6 秒对 142.3 秒），在包括需要环境交互的在线强化学习基准在内的所有方法中，实现了最低的社会车辆延迟（11.3 秒/车）和最少的 EV 停顿次数（1.2 次）。MADT 在更大规模的网格上表现更优，通过图注意力协调在 8x8 网格上实现了 45.2% 的时间缩减，超越了 DT。回报条件化提供了一个平滑的调度接口：通过在 100 到 -400 之间调整目标回报，可以在 EV 行驶时间（72.4-138.2 秒）与社会车辆延迟（16.8-5.4 秒/车）之间进行权衡，且无需重新训练。此外，受限 DT (Constrained DT) 扩展引入了明确的社会车辆干扰预算作为第二个控制维度。

## Abstract
Emergency vehicle (EV) response time is a critical determinant of survival outcomes, yet deployed signal preemption strategies remain reactive and uncontrollable. We propose a return-conditioned framework for emergency corridor optimization based on the Decision Transformer (DT). By casting corridor optimization as offline, return-conditioned sequence modeling, our approach (1) eliminates online environment interaction during policy learning, (2) enables dispatch-level urgency control through a single target-return scalar, and (3) extends to multi-agent settings via a Multi-Agent Decision Transformer (MADT) with graph attention for spatial coordination. On the LightSim simulator, DT reduces average EV travel time by 37.7% relative to fixed-timing preemption on a 4x4 grid (88.6 s vs. 142.3 s), achieving the lowest civilian delay (11.3 s/veh) and fewest EV stops (1.2) among all methods, including online RL baselines that require environment interaction. MADT further improves on larger grids, overtaking DT with 45.2% reduction on 8x8 via graph-attention coordination. Return conditioning produces a smooth dispatch interface: varying the target return from 100 to -400 trades EV travel time (72.4-138.2 s) against civilian delay (16.8-5.4 s/veh), requiring no retraining. A Constrained DT extension adds explicit civilian disruption budgets as a second control knob.

---

## 论文详细总结（自动生成）

这是一份关于论文《Emergency Preemption Without Online Exploration: A Decision Transformer Approach》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不进行危险的在线探索（Online Exploration）且具备可控性的前提下，优化城市交通中的紧急车辆（EV）通行效率，同时最小化对社会车辆（背景交通）的干扰。
*   **研究动机**：
    *   **临床紧迫性**：急救响应时间每延迟1分钟，心脏骤停患者的生存率降低7-10%。
    *   **现有技术缺陷**：目前的信号抢占系统（如GPS/光学触发）多为被动反应，缺乏对下游拥堵的预判；而在线强化学习（RL）在真实交通中训练风险极高，且训练后的策略是静态的，调度员无法根据紧急程度实时调整。
    *   **经济成本**：救护车延迟每年给美国医疗系统造成约23亿美元的额外支出。

### 2. 方法论
*   **核心思想**：将交通信号控制建模为**离线、回报条件化的序列建模问题**，利用 Decision Transformer (DT) 架构，通过预测动作来匹配目标回报。
*   **关键技术细节**：
    *   **DT 架构**：输入序列由（回报、状态、动作）三元组组成。状态编码包括信号相位、车流密度、EV 距离和归一化时间。
    *   **回报条件化（Return Conditioning）**：在推理时，调度员输入一个目标回报标量 $G^*$。高 $G^*$ 产生激进的绿波带，低 $G^*$ 则倾向于保护社会交通。
    *   **多智能体扩展 (MADT)**：引入图注意力网络（GAT）进行空间协调，使各交叉口智能体能感知邻居状态，形成长距离绿波。
    *   **受限 DT (CDT)**：增加“成本到期”（Cost-to-go）标记，允许调度员设定明确的社会车辆延迟预算。
*   **算法流程**：首先从混合质量（专家、随机、噪声）的离线数据中学习，训练时不与环境交互；部署时通过自回归方式生成相位指令。

### 3. 实验设计
*   **数据集/场景**：使用自主开发的 **LightSim** 模拟器（基于细胞传输模型 CTM）。实验设置了 4×4、6×6 和 8×8 三种规模的城市网格。
*   **Benchmark 与对比方法**：
    *   **规则类**：固定配时抢占 (FT-EVP)、贪婪抢占 (Greedy)、最大压力控制 (MaxPressure)。
    *   **在线 RL**：DQN、PPO（训练 50 万步）。
    *   **离线 RL**：CQL（保守 Q 学习）、IQL（隐式 Q 学习）。
*   **评估指标**：EV 行驶时间 (ETT)、平均社会车辆延迟 (ACD)、交通吞吐量、EV 停顿次数。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A100 (80GB)** GPU。
*   **训练时长**：
    *   4×4 网格：约 12.3 分钟。
    *   8×8 网格：约 51.4 分钟。
*   **推理延迟**：单步推理仅需 2.3ms - 8.7ms，远低于 5 秒的控制周期，满足实时性要求。

### 5. 实验数量与充分性
*   **实验规模**：
    *   训练集包含 5,000 个情节（Episodes）。
    *   每种方法在 100 个独立评估情节上取平均值。
*   **充分性与公平性**：
    *   **消融实验**：涵盖了上下文长度、数据质量（全专家 vs 混合）、有无回报条件化、模型深度等。
    *   **鲁棒性测试**：在不同交通需求水平（低、中、高、极高拥堵）下验证了泛化性。
    *   **统计检验**：使用了 Welch's t-test 验证结果的显著性（$p < 0.01$）。
    *   **跨模拟器验证**：通过与 SUMO 模拟器的排名相关性分析（Spearman $\rho=0.95$），证明了实验结果的客观性。

### 6. 主要结论与发现
*   **性能卓越**：在 4×4 网格中，DT 将 EV 行驶时间缩短了 **37.7%**，且社会车辆延迟最低。
*   **可扩展性**：在 8×8 大规模网格中，MADT 凭借空间协调能力，将 EV 时间缩减率提升至 **45.2%**，超越了单体 DT。
*   **灵活可控**：通过调节 $G^*$，实现了从“极速通行”（72.4s）到“保守干扰”（138.2s）的平滑切换，无需重新训练。
*   **轨迹缝合**：DT 能够通过“缝合”离线数据中的优质片段，表现优于其训练数据中最优的专家轨迹。

### 7. 优点
*   **安全性**：纯离线学习，避免了在线训练时可能导致的交通瘫痪或事故风险。
*   **调度接口**：首次为交通控制引入了类似“拨盘”的可控接口（目标回报），符合实际调度需求。
*   **高效性**：相比在线 RL，训练速度快 3-6 倍，且推理延迟极低。
*   **开源贡献**：提供了轻量级模拟器 LightSim，便于后续研究复现。

### 8. 不足与局限
*   **模拟器简化**：LightSim 基于宏观 CTM 模型，未考虑微观层面的换道、加减速细节及驾驶员对警笛的反应。
*   **路径固定**：假设 EV 路径预先已知，未涉及动态路径规划与信号控制的联合优化。
*   **相位转换**：模型简化了黄灯和全红清空时间，可能略微高估了实际通行能力。
*   **分布偏移风险**：当设定的目标回报远超训练数据分布时，模型行为可能变得不稳定。

（完）
