---
title: Column Generation for the Micro-Transit Zoning Problem
title_zh: 求解微循环公交分区问题的列生成算法
authors: "Hins Hu, Rishav Sen, Jose Paolo Talusan, Abhishek Dubey, Aron Laszka, Samitha Samaranayake"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07821v1"
tags: ["query:urt-c"]
score: 8.0
evidence: 列生成算法用于微循环公交分区与规划
tldr: 随着城市出行需求多样化，微公交（Micro-transit）作为连接固定线路公交与网约车的桥梁，在提升可持续性和交通公平性方面具有重要意义。针对微公交运营中关键的地理围栏区域规划问题（MZP），本文提出了一种广义模型，打破了传统方法对候选区域大小的限制，引入全局预算约束。研究设计了列生成（Column Generation）算法框架及多种定价启发式算法，在全美多个城市的实验证明，该方法在求解质量、效率和扩展性上均优于现有方案。
selection_source: carryover_cache
motivation: 现有的微公交区域规划方法通常需要预先枚举候选区域且受限于固定区域大小，难以应对复杂的全局预算约束和大规模优化需求。
method: 本文将微公交区域规划问题广义化，并提出了一种基于列生成（Column Generation）的算法框架，结合多种定价启发式策略以加速计算。
result: 在美国主要城市的数值实验表明，该方法能够产生更高质量的规划方案，且在计算效率和处理大规模问题的能力上表现更优。
conclusion: 该研究为微公交系统的区域规划提供了更灵活且高效的优化工具，有助于提升城市交通系统的可持续性与服务覆盖范围。
---

## 摘要
随着过去十年中拼车等新型城市出行方式的快速发展，按需微循环公交服务作为一种折中方案脱颖而出，填补了固定线路公共交通与单次请求网约车之间的空白，在最大化客流量与最小化旅行时间之间取得了平衡。采用微循环公交具有显著的社会影响。由于其人均行驶里程更低、时间表灵活且价格实惠，它在降低能耗和减少排放、提高城市可持续性的同时，也增强了弱势群体获得公平出行服务的机会。然而，微循环公交服务的有效运营需要提前规划地理围栏区域，这涉及解决一个具有挑战性的组合优化问题。现有方法通常先枚举候选区域，然后在第二步中选择固定数量的最佳区域。在本文中，我们将微循环公交分区问题（MZP）进行了推广，允许设定全局预算，而不是对候选区域施加大小限制。我们还设计了一个列生成（CG）框架来解决该问题，并提出了几种定价启发式算法以加速计算。在美国主要城市进行的广泛数值实验表明，我们的方法能更高效地产生更高质量的解，并且在推广后的设定下具有更好的可扩展性。

## Abstract
Along with the rapid development of new urban mobility options like ride-sharing over the past decade, on-demand micro-transit services stand out as a middle ground, bridging the gap between fixed-line mass transit and single-request ride-hailing, balancing ridership maximization and travel time minimization. Micro-transit adoption can have significant social impact. It improves urban sustainability, through lower energy consumption and reduced emissions, while enhancing equitable mobility access for disadvantaged communities, thanks to its lower vehicle miles per passenger, flexible schedules, and affordable pricing. However, effective operation of micro-transit services requires planning geo-fenced zones in advance, which involves solving a challenging combinatorial optimization problem. Existing approaches enumerate candidate zones first and selects a fixed number of optimal zones in the second step. In this paper, we generalize the Micro-Transit Zoning Problem (MZP) to allow a global budget rather than imposing a size limit for candidate zones. We also design a Column Generation (CG) framework to solve the problem and several pricing heuristics to accelerate computation. Extensive numerical experiments across major U.S. cities demonstrate that our approach produces higher-quality solutions more efficiently and scales better in the generalized setting.

---

## 论文详细总结（自动生成）

这篇论文针对城市微循环公交（Micro-transit）的分区规划问题，提出了一种基于列生成（Column Generation）的高效优化框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：微循环公交作为固定线路公交与网约车之间的“中间地带”，在提升城市交通可持续性和社会公平性方面具有重要作用。然而，如何科学地划分地理围栏服务区（Zoning）是一个复杂的组合优化问题。
*   **核心问题**：现有的分区方法（如 CliqueGen）通常假设区域数量固定且需预先枚举所有候选区域，这在面对大规模城市数据或复杂的全局预算约束时，存在严重的计算瓶颈和扩展性问题。
*   **研究目标**：提出一个更符合实际的广义微循环公交分区问题（MZP）模型，引入全局运营预算约束，并开发一种能够处理指数级候选区域空间的算法。

### 2. 方法论
*   **核心思想**：利用**列生成（Column Generation, CG）**算法将复杂的分区问题分解为两个子问题：受限主问题（RMP）和定价问题（Pricing Problem）。
*   **关键技术细节**：
    *   **受限主问题 (RMP)**：在已生成的候选区域集合中，选择一组区域以最大化总需求覆盖，同时满足全局预算约束。
    *   **定价问题 (Pricing Problem)**：寻找具有正“检验数”（Reduced Cost）的新区域（即“列”）。论文将其建模为整数线性规划（ILP）或整数二次规划（IQP）。
    *   **定价启发式算法**：为了加速计算，设计了一种基于**随机种子的贪心节点添加策略**。该算法从随机单元对开始，迭代添加能最大化边际收益（需求增益减去成本增量）的单元，直到无法进一步优化。
    *   **整体流程**：交替求解 RMP 和定价问题，不断将优质区域加入候选集，直至收敛或达到时间限制。

### 3. 实验设计
*   **数据集与场景**：选取了美国 5 个具有代表性的中大型城市：迈阿密、波士顿、亚特兰大、查塔努加和纳什维尔。
*   **空间粒度**：使用 Uber H3 空间索引，分为 Resolution 7（约 1.22km）和 Resolution 8（约 0.46km）两种分辨率。
*   **基准方法 (Benchmark)**：对比了目前最先进的微循环分区算法 **CliqueGen**（Hu et al., 2025）。
*   **对比维度**：需求覆盖率（Solution Quality）、计算耗时（Efficiency）以及在大规模实例下的可扩展性。

### 4. 资源与算力
*   **硬件环境**：实验在 AWS `c6a.xlarge` 实例上运行，配置为 **4 核 CPU 和 8 GiB 内存**。
*   **软件工具**：使用 **Gurobi** 作为数学规划求解器。
*   **时间限制**：精确列生成设置了 10 小时上限，启发式列生成设置了 20 分钟上限。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个城市、2 种空间分辨率，共计 10 组核心配置。
*   **消融与对比实验**：
    *   对比了精确定价（ILP/IQP）与启发式定价的效果。
    *   对比了定价问题中 ILP 与 IQP 两种数学表述的求解效率。
    *   分析了启发式算法中随机运行次数（R）对结果的影响。
*   **充分性评价**：实验设计较为全面，不仅验证了算法在不同城市规模下的表现，还深入探讨了算法内部组件的性能差异，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能优越性**：在 20 分钟的相同限时内，CG 框架在大型实例（如纳什维尔）中的需求覆盖率远超 CliqueGen（87% vs 0.37%）。
*   **可扩展性**：CliqueGen 在处理超过 200 个单元的城市时常因内存溢出（OOM）失败，而 CG 框架能稳定处理包含 1800+ 单元的大规模问题。
*   **启发式效率**：定价启发式算法比精确 ILP 求解快约 30 倍，且解的质量损失极小（几乎一致）。
*   **数学模型发现**：在精确求解定价问题时，IQP 表述比线性化后的 ILP 表述快 2-4 倍，因为 ILP 引入了过多的配对变量。

### 7. 优点
*   **模型更贴近现实**：引入全局预算约束而非固定区域数量，更符合城市规划者的实际决策逻辑。
*   **算法创新**：首次将列生成算法应用于微循环公交分区，有效解决了候选区域爆炸带来的计算难题。
*   **落地性强**：与实际公交机构（CARTA）合作，使用了真实出行数据，并考虑了道路网络距离而非简单的欧几里得距离。

### 8. 不足与局限
*   **模型简化**：目前仅考虑了区域内（Intra-zone）需求，未充分考虑区域间（Inter-zone）以及微公交与固定线路公交之间的接驳协同。
*   **成本函数单一**：运营成本仅基于区域直径的平方，实际情况中可能涉及车辆数、排班等更复杂的成本因素。
*   **最优性保证**：由于未采用完整的 Branch-and-Price（仅在根节点进行列生成），最终获得的是整数解的近似，而非理论全局最优解。

（完）
