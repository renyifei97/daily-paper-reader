---
title: Efficient Dense Crowd Trajectory Prediction Via Dynamic Clustering
title_zh: 基于动态聚类的高效密集人群轨迹预测
authors: "Antonius Bima Murti Wijaya, Paul Henderson, Marwa Mahmoud"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18166v1"
tags: ["query:urt-c"]
score: 9.0
evidence: 用于管理的高密度人群轨迹预测
tldr: 本研究针对公共安全中的密集人群轨迹预测问题，指出传统个体预测方法在海量且高噪声数据下计算成本过高。为此，提出一种基于动态聚类的即插即用方法，通过将属性相似的个体实时分组并以群体质心作为预测输入，显著降低了计算负担。实验证明，该方法在保持预测精度的同时，大幅提升了处理速度并减少了内存消耗，为密集场景下的灾害预防提供了高效的技术支持。
selection_source: fresh_fetch
motivation: 针对密集人群场景中个体轨迹预测计算开销大且易受追踪噪声干扰的问题，寻求更高效的自动化预测方案。
method: 提出一种动态聚类框架，将行为相似的个体归类并利用群体质心代替个体数据输入现有的预测模型。
result: 在多个挑战性密集人群场景中，该方法在维持预测准确性的前提下，显著提高了处理速度并降低了内存占用。
conclusion: 该即插即用的聚类方法为大规模密集人群的实时监控与安全管理提供了一种兼顾效率与精度的可行路径。
---

## 摘要
人群轨迹预测在公共安全与管理中起着至关重要的作用，有助于预防踩踏等灾难。近期的研究工作通过预测个体轨迹并基于人工标注数据考虑周围物体来解决这一问题。然而，这些方法往往忽略了密集人群场景，由于跟踪输出的大规模性、噪声和不准确性，自动化挑战变得更加突出，从而导致高昂的计算成本。为了应对这些挑战，我们提出并广泛评估了一种新颖的基于聚类的方法，该方法根据随时间变化的相似属性对个体进行分组，通过准确的群体总结实现更快的执行速度。我们的即插即用方法可以通过使用我们输出的质心代替其行人输入，与现有的轨迹预测器相结合。我们在几个具有挑战性的密集人群场景中评估了所提出的方法。我们证明，与最先进的方法相比，我们的方法在保持准确性的同时，实现了更快的处理速度和更低的内存占用。

## Abstract
Crowd trajectory prediction plays a crucial role in public safety and management, where it can help prevent disasters such as stampedes. Recent works address the problem by predicting individual trajectories and considering surrounding objects based on manually annotated data. However, these approaches tend to overlook dense crowd scenarios, where the challenges of automation become more pronounced due to the massiveness, noisiness, and inaccuracy of the tracking outputs, resulting in high computational costs. To address these challenges, we propose and extensively evaluate a novel cluster-based approach that groups individuals based on similar attributes over time, enabling faster execution through accurate group summarisation. Our plug-and-play method can be combined with existing trajectory predictors by using our output centroid in place of their pedestrian input. We evaluate our proposed method on several challenging dense crowd scenes. We demonstrated that our approach leads to faster processing and lower memory usage when compared with state-of-the-art methods, while maintaining the accuracy

---

## 论文详细总结（自动生成）

以下是对论文《Efficient Dense Crowd Trajectory Prediction Via Dynamic Clustering》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在密集人群场景下，传统的个体轨迹预测方法面临三大挑战：
    1.  **计算开销巨大**：随着行人数量增加，考虑邻域交互的计算复杂度呈指数级增长。
    2.  **追踪噪声干扰**：密集场景中遮挡严重，自动追踪算法常出现身份切换（ID-switch）和目标丢失，导致预测输入极不准确。
    3.  **隐私风险**：直接处理个体精细轨迹可能涉及隐私泄露。
*   **研究动机**：受人类在人群中倾向于模仿邻居行为、成组移动的启发，作者提出通过“聚类”来简化数据，将群体视为单一实体进行预测，从而在保证精度的前提下提升效率。

### 2. 论文提出的方法论
该方法是一个**即插即用（Plug-and-Play）**的框架，主要包含以下核心步骤：
*   **嵌套动态聚类（Nested Dynamic Clustering）**：
    *   **第一阶段**：基于**方向角（Direction Angle）**进行凝聚层次聚类，确保同组行人移动方向一致。
    *   **第二阶段**：在方向聚类的基础上，基于**空间距离（Location）**进行二次聚类，限制簇的物理范围。
*   **动态维护机制**：
    *   使用**局部离群因子（LOF）**算法每10帧评估一次成员资格。
    *   识别出的离群点会被重新分配到邻近簇或放入临时列表，当临时列表达到一定数量时重新形成新簇。
*   **平滑质心轨迹计算（Centroid Calculation）**：
    *   为了避免因成员变动导致的质心位置“跳变”，作者不直接计算成员平均位置，而是计算成员的**平均位移增量（Delta）**来更新质心位置，确保轨迹的连续性和平滑性。
*   **集成预测**：将生成的簇质心轨迹输入现有的预测模型（如 Trajectron++），代替原始的个体轨迹。

### 3. 实验设计
*   **数据集**：主要使用 **MOT Head Tracking 21 (HT21)** 数据集，该数据集包含极高密度的头部追踪场景（如商场、教室）。
*   **对比基准（Benchmarks）**：
    *   **数据源对比**：对比了地面真值（GT）、自动追踪输出（HeadHunter-T）以及聚类后的质心数据。
    *   **模型对比**：集成了三种最先进（SOTA）的预测模型：**Trajectron++**、**SocialVAE** 和 **MART**。
    *   **基线方法**：对比了“随机选择（Random Selection）”子样本的朴素降采样方法。
*   **评估指标**：除了标准的 ADE（平均位移误差）和 FDE（最终位移误差），还提出了 CTEO（轨迹错误发生率）、CTEL（轨迹错误长度）和 CMDD（成员距离偏差）等新指标来评估聚类质量。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号（如 RTX 3090/4090）或训练的具体时长。
*   **效率量化**：论文重点展示了**执行时间（Execution Time）**和**内存占用（Memory Usage）**的减少。例如，在某些场景下执行时间减少了 33% 至 79%，内存占用最高减少了约 43%。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 HT21 的 3 个核心密集场景（02, 03, 04）进行了详尽测试。
    *   涵盖了短时（12步）和长时（50步）预测。
    *   每组实验重复 10 次并计算标准差（2-sigma 误差）。
*   **充分性评价**：实验设计较为充分且客观。通过对比 GT 和自动追踪数据，证明了该方法在现实噪声环境下的鲁棒性；通过对比随机采样，证明了聚类总结信息的有效性而非简单的降采样。

### 6. 论文的主要结论与发现
*   **效率大幅提升**：动态聚类显著降低了预测模型的计算负担，尤其是在处理数千个节点的密集图结构时。
*   **精度保持良好**：在短时预测中，聚类方法的精度与原始追踪数据相当；在**长时预测**中，SocialVAE 结合聚类后的表现甚至优于原始追踪数据，说明聚类起到了“去噪”的作用。
*   **鲁棒性**：该方法能有效处理追踪过程中的 ID 丢失和切换问题，通过群体总结弥补了个体数据的缺失。

### 7. 优点
*   **即插即用**：不需要修改现有预测模型的架构，具有极强的通用性。
*   **去噪能力**：通过聚合行为相似的个体，抵消了单体追踪的不稳定性。
*   **隐私保护**：输出的是聚合后的群体质心，减少了对个体敏感位置数据的依赖。
*   **平滑性设计**：提出的 Delta 位移更新算法有效解决了动态聚类中常见的质心抖动问题。

### 8. 不足与局限
*   **短时精度微降**：在极精细的短时预测中，由于丢失了个体特有的微小交互信息，ADE/FDE 相比 GT 略有上升。
*   **阈值敏感性**：聚类效果依赖于预设的距离和角度阈值，不同密度的场景可能需要手动调优。
*   **极端个体行为丢失**：如果某个体采取了完全异于常人的避障路径，可能会被 LOF 识别为噪声或被强行归类，导致极端异常行为的预测失效。
*   **硬件细节缺失**：缺乏具体硬件环境下的端到端延迟数据，难以评估其在嵌入式设备上的实时性表现。

（完）
