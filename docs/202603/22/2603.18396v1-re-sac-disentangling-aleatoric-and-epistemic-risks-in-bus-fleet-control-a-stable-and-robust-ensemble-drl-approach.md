---
title: "RE-SAC: Disentangling aleatoric and epistemic risks in bus fleet control: A stable and robust ensemble DRL approach"
title_zh: RE-SAC：解耦公交车队控制中的偶然风险与认知风险：一种稳定且鲁棒的集成深度强化学习方法
authors: "Yifan Zhang, Liang Zheng"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18396v1"
tags: ["query:urt-c"]
score: 8.0
evidence: 用于公交车队驻留控制和乘客需求的集成深度强化学习
tldr: 针对公交驻留控制中随机交通和乘客需求带来的不确定性，本文提出了一种鲁棒集成软演员-评论家（RE-SAC）框架。该方法通过解耦偶然不确定性（噪声）和认知不确定性（数据不足），利用基于IPM的权重正则化和多样化Q集成，解决了标准DRL在波动环境下的Q值不稳定和策略崩溃问题，显著提升了公交调度的鲁棒性和效率。
selection_source: fresh_fetch
motivation: 传统深度强化学习在公交控制中因混淆偶然与认知不确定性，导致Q值估计不稳和策略失效。
method: 提出RE-SAC框架，结合IPM权重正则化应对偶然风险，并利用多样化Q集成惩罚认知风险，实现两种不确定性的显式解耦。
result: "在双向公交走廊模拟中，RE-SAC的累积回报显著优于基准算法，且在稀有分布外状态下的Q值估计误差降低了高达62%。"
conclusion: 通过解耦不同来源的风险，RE-SAC为高波动环境下的公交车队控制提供了一种稳定且鲁棒的深度强化学习解决方案。
---

## 摘要
由于随机的交通状况和乘客需求，公交驻留控制具有挑战性。尽管深度强化学习（DRL）展现出应用前景，但标准的 Actor-Critic 算法在多变的环境中面临 Q 值不稳定的问题。这种不稳定性的一个关键来源是混淆了两种截然不同的不确定性：偶然不确定性（不可约噪声）和认知不确定性（数据不足）。将这两者视为单一风险会导致在噪声状态下的价值低估，从而引发灾难性的策略崩溃。我们提出了一种鲁棒集成软 Actor-Critic（RE-SAC）框架，以显式解耦这些不确定性。RE-SAC 对 Critic 网络应用基于积分概率度量（IPM）的权重正则化以规避偶然风险，在无需昂贵的内循环扰动的情况下，为鲁棒贝尔曼算子提供平滑的解析下界。为了应对认知风险，多样化的 Q 集成对数据覆盖稀疏区域中过度自信的价值估计进行惩罚。这种双重机制防止了集成方差将噪声误认为数据缺口，这是我们在消融实验中发现的一种失效模式。在真实的双向公交走廊模拟实验中，结果表明 RE-SAC 相比于原始 SAC（约 -0.55e6）实现了最高的累积奖励（约 -0.4e6）。马氏稀有度分析证实，在稀有的分布外状态下，RE-SAC 将 Oracle Q 值估计误差降低了高达 62%（MAE 为 1647，而对比组为 4343），证明了其在高交通波动性下的卓越鲁棒性。

## Abstract
Bus holding control is challenging due to stochastic traffic and passenger demand. While deep reinforcement learning (DRL) shows promise, standard actor-critic algorithms suffer from Q-value instability in volatile environments. A key source of this instability is the conflation of two distinct uncertainties: aleatoric uncertainty (irreducible noise) and epistemic uncertainty (data insufficiency). Treating these as a single risk leads to value underestimation in noisy states, causing catastrophic policy collapse. We propose a robust ensemble soft actor-critic (RE-SAC) framework to explicitly disentangle these uncertainties. RE-SAC applies Integral Probability Metric (IPM)-based weight regularization to the critic network to hedge against aleatoric risk, providing a smooth analytical lower bound for the robust Bellman operator without expensive inner-loop perturbations. To address epistemic risk, a diversified Q-ensemble penalizes overconfident value estimates in sparsely covered regions. This dual mechanism prevents the ensemble variance from misidentifying noise as a data gap, a failure mode identified in our ablation study. Experiments in a realistic bidirectional bus corridor simulation demonstrate that RE-SAC achieves the highest cumulative reward (approx. -0.4e6) compared to vanilla SAC (-0.55e6). Mahalanobis rareness analysis confirms that RE-SAC reduces Oracle Q-value estimation error by up to 62% in rare out-of-distribution states (MAE of 1647 vs. 4343), demonstrating superior robustness under high traffic variability.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：RE-SAC 框架

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在城市公交系统中，由于交通流和乘客需求的固有随机性，公交车容易出现“串车”（Bus Bunching）现象。现有的深度强化学习（DRL）方法在处理此类高波动环境时，往往会出现 **Q值中毒（Q-value poisoning）** 或 **价值坍塌**，导致策略失效。
*   **研究动机**：作者认为这种不稳定性源于对两种不确定性的混淆：
    1.  **偶然风险（Aleatoric Risk）**：环境固有的、不可约的噪声（如信号灯随机性）。
    2.  **认知风险（Epistemic Risk）**：由于经验回放池中数据覆盖不足导致的模型知识缺失。
*   **整体含义**：本文旨在通过解耦这两种风险，提出一种鲁棒且稳定的集成 DRL 框架（RE-SAC），以提升公交驻留控制在极端和多变环境下的可靠性。

#### 2. 核心方法论
*   **核心思想**：将鲁棒马尔可夫决策过程（RMDP）与集成学习结合，通过不同的数学手段分别抑制两种风险。
*   **关键技术细节**：
    *   **偶然风险缓解（IPM 正则化）**：利用积分概率度量（IPM）理论，证明了对 Critic 网络权重进行 **$\ell_1$ 范数正则化** 等价于在 Wasserstein 球内寻求鲁棒下界。这种方法无需复杂的内循环状态扰动，计算效率高。
    *   **认知风险缓解（Q-Ensemble）**：维护 $K$ 个独立的 Q 网络集成。利用集成方差作为认知不确定性的代理指标，在贝尔曼更新中减去该方差（悲观估计），确保智能体在未探索区域保持谨慎。
    *   **解耦算子（REV Operator）**：定义了鲁棒集成价值算子，将奖励函数减去“认知惩罚项”和“偶然惩罚项”，从而在保持高价值轨迹的同时，过滤环境噪声。
    *   **Actor 更新**：采用置信下界（LCB）目标函数，结合分类嵌入（Categorical Embeddings）处理公交 ID 和站点 ID 等离散特征。

#### 3. 实验设计
*   **实验场景**：一个高保真的双向公交走廊模拟环境，包含 22 个站点，模拟 13 小时（06:00-19:00）的服务窗口。
*   **数据特征**：乘客到达服从泊松分布，路段旅行速度服从高斯分布（$\sigma = 1.5$ m/s）。
*   **Benchmark（基准方法）**：
    *   **Vanilla SAC**：标准软演员-评论家算法。
    *   **DSAC**：分布深度强化学习，用于处理风险敏感性。
    *   **BAC (BEE Actor-Critic)**：旨在通过利用过去成功经验来避免价值低估的先进算法。
*   **消融实验**：包括“仅认知风险”变体、“仅偶然风险”变体，以及不同集成规模 $K$（2 到 40）的对比。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号（如 NVIDIA A100 或 RTX 3090）及具体的训练总时长。
*   **实现细节**：提到使用 PyTorch 实现，并采用了 **向量化 Critic 模块（VectorizedCritic）** 以提升多网络集成的计算效率。批量大小（Batch Size）设为 2048，经验池容量为 $10^6$。

#### 5. 实验数量与充分性
*   **实验规模**：每个模型训练 500 个 Episode，评估结果基于 10 次独立运行的平均值。
*   **充分性评价**：
    *   **充分性**：实验设计较为全面，不仅对比了累积回报，还深入分析了 Q 值估计的稳定性。
    *   **客观性**：引入了 **马氏稀有度分析（Mahalanobis Rareness）**，在分布外（OOD）状态下对比 Oracle MAE（真实误差），这在交通控制类论文中属于非常严谨的鲁棒性验证。
    *   **公平性**：所有基准算法共享相同的网络架构、学习率和缓冲区配置。

#### 6. 主要结论与发现
*   **性能领先**：RE-SAC 实现了最高且最稳定的累积奖励（约 $-0.4 \times 10^6$），显著优于 SAC（$-0.55 \times 10^6$）。
*   **防止坍塌**：消融实验显示，仅考虑认知风险会导致模型在噪声环境下产生过度悲观，引发策略灾难性坍塌；而 RE-SAC 通过解耦成功避免了这一陷阱。
*   **OOD 鲁棒性**：在罕见交通状态下，RE-SAC 的 Q 值估计误差比 SAC 降低了 **62%**，证明其在极端情况下的决策更可靠。
*   **集成规模效应**：中等规模的集成（$K=5, 10, 20$）效果最好，过大的集成（$K=40$）可能因过度保守而略微抑制性能。

#### 7. 优点（亮点）
*   **理论严谨**：将复杂的风险敏感 RL 问题转化为简单的权重正则化问题，具有坚实的数学基础（IPM 理论与 RMDP）。
*   **计算高效**：相比于需要对抗攻击或内循环优化的鲁棒 RL，RE-SAC 的解析下界方法几乎不增加额外的计算负担。
*   **实用性强**：针对公交控制中“噪声”与“数据稀疏”共存的痛点，提供了切实可行的解耦方案。

#### 8. 不足与局限
*   **超参数敏感性**：算法引入了多个风险系数（$\lambda_{ale}, \lambda_{epi}, \beta_{lcb}$），这些参数的调节可能需要大量的领域经验。
*   **场景单一**：实验仅限于单条双向走廊，尚未在复杂的城市级多线路网络（涉及换乘协调）中验证。
*   **单智能体局限**：虽然使用了嵌入向量处理多车信息，但在极大规模车队中，单智能体架构可能面临状态空间爆炸的挑战，未来可能需要向多智能体（MARL）扩展。

（完）
