Title: Residuals-based Offline Reinforcement Learning

URL Source: https://arxiv.org/pdf/2604.01378v1

Published Time: Fri, 03 Apr 2026 00:08:48 GMT

Number of Pages: 8

Markdown Content:
# Residuals-based Offline Reinforcement Learning 

Qing Zhu 1 and Xian Yu 2

Abstract — Offline reinforcement learning (RL) has received increasing attention for learning policies from previously col-lected data without interaction with the real environment, which is particularly important in high-stakes applications. While a growing body of work has developed offline RL algorithms, these methods often rely on restrictive assumptions about data coverage and suffer from distribution shift. In this paper, we propose a residuals-based offline RL framework for general state and action spaces. Specifically, we define a residuals-based Bellman optimality operator that explicitly incorporates estimation error in learning transition dynamics into policy optimization by leveraging empirical residuals. We show that this Bellman operator is a contraction mapping and identify conditions under which its fixed point is asymptotically optimal and possesses finite-sample guarantees. We further develop a residuals-based offline deep Q-learning (DQN) algorithm. Using a stochastic CartPole environment, we demonstrate the effectiveness of our residuals-based offline DQN algorithm. 

I. INTRODUCTION Sequential decision-making under uncertainty is funda-mental to many real-world applications. In these problems, agents need to make decisions through interacting with the environment to minimize some cumulative costs (or maximize some cumulative rewards). Markov decision pro-cesses (MDPs) provide a natural framework for modeling such problems [1], and reinforcement learning (RL) has become a popular tool for solving them [2]. However, online RL is rarely deployed in high-stakes applications, such as transportation, energy, and healthcare systems, due to its re-liance on interaction with the real environment. Specifically, online RL relies on trial-and-error learning, during which the decisions may be highly suboptimal, potentially resulting in catastrophic outcomes. In this paper, we focus on the offline 

RL setting instead, where learning is based on a static logged dataset collected under a behavior policy. We refer interested readers to an extensive review on offline RL [3]. The essential limitations in offline RL are threefold. First, since the learning algorithm entirely relies on the static offline dataset, this dataset must have sufficient coverage over the state-action space. A common assumption requires that the state-action distribution induced by the target policy is absolutely continuous with respect to the behavior distribu-tion, with a uniformly bounded density ratio (often referred to as a concentrability coefficient ) [4]–[7]. This assumption is often prohibitive in practice, especially for problems with  

> This work was supported in part by the National Science Foundation under Grant #2331782.
> 1Department of Integrated Systems Engineering, The Ohio State Univer-sity, Columbus, OH, USA,

zhu.2166@osu.edu  

> 2Corresponding author; Department of Integrated Systems Engineering, The Ohio State University, Columbus, OH, USA,

yu.3610@osu.edu 

continuous state and/or action spaces, as it requires the data distribution to uniformly cover all reachable states. Second, offline RL is often concerned about counterfactual queries, i.e., asking what might have happened if the agent had implemented a different action than the one in the dataset. This is impossible in the high-stakes settings, as it is impractical to experiment with different decisions solely to observe the resulting state. Third, a fundamental challenge in offline RL is the distribution shift between the behavior policy governing the offline dataset and the policy being trained, leading to unreliable policy evaluation. Distribution shift can be addressed by constraining the distance between the learned policy and the behavior policy [8]–[12]. However, since the past decisions in the offline dataset may be sub-optimal, enforcing such constraints may prevent the learned policy from achieving optimal performance. In this paper, we propose a residuals-based offline RL framework for general state and action spaces. In contextual stochastic optimization, [13] proposed an empirical residuals-based sample average approximation (ER-SAA) for single-stage and two-stage stochastic programs that directly incor-porates the estimation error in predicting the uncertainty into the downstream optimization problem. Leveraging this idea, in this work, we first construct an estimated transition model from the static offline data using supervised learning. Based on the observed transitions, we compute the empirical residuals, which capture the discrepancy between the learned model and the true system dynamics. We generate trajec-tories by sampling residuals and adding them to the point prediction of the next state, and then train our policies using these generated trajectories. This framework addresses the current limitations in offline RL in the following ways. First, different than the vast literature, we do not require the dataset to cover all the state-action pairs, as the sampling procedure will automatically generate states that are unseen in the dataset due to the empirical residuals. Second, this residuals-based framework emulates real-world dynamics and provides approximate answers to counterfactual queries. Third, this framework addresses distribution shift by collecting data following the learned policy and using it to evaluate the value function, enabling on-policy training. The rest of this paper is organized as follows. We first introduce our infinite-horizon discounted MDP setting and review ER-SAA in Section II. In Section III, we develop the residuals-based offline RL framework and establish its consistency and finite sample guarantees. For practical use, we also design a residuals-based offline deep Q-learning (DQN) algorithm. Finally, we present our numerical results on a stochastic CartPole environment in Section IV. 

> arXiv:2604.01378v1 [cs.LG] 1 Apr 2026

II. PRELIMINARIES 

A. Infinite-horizon Discounted MDPs 

We consider an infinite-horizon γ-discounted MDP repre-sented by the tuple (S, A, P, c, γ ), where S is the convex state space, A is the compact (possibly continuous) action space, P (·| s, a ) is the conditional density of the transition distribution, c(s, a ) is the deterministic immediate cost func-tion 1, and γ ∈ [0 , 1) is the discount factor. Note that we require a convex state space to ensure the Lipschitz conti-nuity of the orthogonal projection Π, i.e., ∥ΠS a − ΠS b∥ ≤ ∥a − b∥, ∀a, b .A stationary deterministic Markov policy is a mapping 

π : S → A , which specifies the action a = π(s) in state 

s. Let M denote the set of all stationary deterministic Markov policies. For any policy π ∈ M , the corresponding state value function and state-action value function are defined as V π (s) := Eπ [P∞ 

> t=0

γtc(st, a t)|s0 = s] , at =

π(st), st+1 ∼ P (·| st, a t) and Qπ (s, a ) := 

Eπ [P∞ 

> t=0

γtc(st, a t) | s0 = s, a 0 = a] , a t = π(st), s t+1 ∼

P (·| st, a t), respectively. It is well known that there exists an optimal stationary deterministic Markov policy in risk-neutral RL [1]. Without loss of optimality, our goal is to find a policy π ∈ M that minimizes the expected total discounted cost: min π∈M V π (s).Denote the optimal policy as π⋆ and the optimal value function and state-action value function by V ⋆(s) and 

Q⋆(s, a ), where V ⋆(s) = min a∈A Q⋆(s, a ) = Q⋆(s, π ⋆(s)) .According to [1], they satisfy the following Bellman equa-tions 

V ⋆(s) = min  

> a∈A

{c(s, a ) + γ E[V ⋆(s′) | s′ ∼ P (· | s, a )] } ,Q⋆(s, a ) = c(s, a ) + γ E



min  

> a′∈A

Q⋆(s′, a ′) s′ ∼ P (· | s, a )



.

Let f ⋆ denote the ground-truth (unknown) transition func-tion such that s′ = f ⋆(s, a ) + ϵ, where ϵ ∼ Pϵ is the zero-mean random noise. Then, the conditional density 

P (·| s, a ) can be equivalently converted to the density Pϵ.Specifically, let T ⋆ denote the Bellman optimality opera-tor and B(S, A) denote the set of all bounded measur-able real-valued functions on S × A with supremum norm 

∥Q∥∞ := sup (s,a )∈S×A |Q(s, a )|. Then, for any function 

Q ∈ B (S, A), we have 

(T ⋆Q)( s, a ) := c(s, a ) + γ

Z

> S

min  

> a′∈A

Q(s′, a ′) P (d s′ | s, a )= c(s, a ) + γ

Z

min  

> a′∈A

Q f ⋆(s, a ) + ϵ, a ′ Pϵ(dϵ ). (1) From [1], the Bellman optimality operator defined in (1) is a contraction mapping, where Q⋆ is its unique fixed point. 

Offline RL. In this paper, we study an offline RL setting in which learning is based on a fixed batch dataset DN =

{(s(i), a (i), c (i), s ′(i))}Ni=1 instead of on direct interaction with the environment. The dataset is generated according 

> 1Our framework can be extended to incorporate stochastic immediate cost with relative ease.

to a behavior distribution db and the transition kernel P ,i.e., (s(i), a (i)) ∼ db, c(i) = c(s(i), a (i)), s′(i) ∼ P (· | 

s(i), a (i)), i = 1, . . . , N. Given a test state distribution 

ρ ∈ ∆( S), the aim of offline RL is to learn a policy π

such that for a prescribed accuracy level ϵ > 0, we have 

V ⋆(ρ) − V π (ρ) ≤ ϵ with high probability. 

B. ER-SAA 

Next, we review the ER-SAA proposed in [13]. Contextual stochastic optimization studies decision-making under uncer-tainty by leveraging contextual information that affects the distribution of random outcomes. Specifically, given a new observation of the random contextual information X = x, we aim to solve the following stochastic optimization problem 

min z∈Z E[c(z, Y )|X = x], where z ∈ Z denotes the decision variables, Y ∈ Y denotes the uncertainty, c is a cost function, and the expectation is computed with respect to the distribution of Y conditioned on X = x.Assume that the true relationship between the random vector Y and the random covariates X is given by Y =

f ⋆(X) + ε, where f ⋆(x) := E[Y |X = x] is the true regression function, and ε is a zero-mean random error independent of X. In practice, f ⋆ is usually unknown and can be estimated from some dataset DN = {(xi, y i)}Ni=1 .In [13], the authors first estimate f ⋆ with ˆfN based on dataset DN and define the empirical residuals as ˆϵkN := 

yk − ˆfN (xk), k = 1 , . . . , N. Using ˆfN and the empirical residuals, one can construct the ER-SAA as follows: 

min 

> z∈Z

1

N

> N

X

> k=1

c



z, ΠY

  ˆfN (x) + ˆ ϵkN



, (2) where ΠY ensures that the predicted scenarios lie within Y.In [13], the authors derived conditions under which the solutions of the ER-SAA (2) are asymptotically optimal and possess finite sample guarantees. However, this framework has only been discussed in the single-stage or two-stage set-ting when the uncertainty is solely affected by the contextual information. Next, we apply the empirical residuals-based framework to offline RL, where the state can be affected by both the previous state and action, and we learn such transition dynamics using an offline dataset. III. RESIDUALS-BASED OFFLINE RL In this section, we introduce the residuals-based offline RL framework (see Fig. 1), which consists of two main steps: (i) Regression , where we estimate the transition function based on the offline dataset using supervised learning and construct the empirical residuals and (ii) Residuals-based offline RL , where we generate next states by sampling empirical residuals and adding them to the point prediction and train policies based on the generated trajectories. Next, we first define the residuals-based and full-information Bellman optimality operators and study the property of their fixed points in Section III-A. Then we prove the consistency and finite sample guarantees of the residuals-based offline RL framework in Section III-B. Fig. 1: Flowchart of the residuals-based offline RL. 

A. Residuals-based Bellman Optimality Operator 

Because the true transition function f ⋆ is unknown, we estimate it from the offline dataset 

DN = {(s(i), a (i), c (i), s ′(i))}Ni=1 using supervised learning. Let F denote a function class that contains f ∗2. Given dataset 

DN and some loss function ℓ : S × S → R+, we obtain an estimator ˆfN by solving an empirical risk minimization problem ˆfN ∈ arg min f ∈F 1

> N

PNi=1 ℓ f (s(i), a (i)), s ′(i).We then construct the empirical residuals ˆϵ(i) 

> N

:= 

s′(i) − ˆfN (s(i), a (i)), i = 1, . . . , N , which measure the discrepancy between the observation and the predicted next state. Finally, given any state-action pair (s, a ), we add these N empirical residuals to the point prediction of the next state and project them onto the state space S, i.e., 

ΠS ( ˆfN (s, a ) + ˆ ϵ(i) 

> N

), ∀i = 1 , . . . , N . We then approximate the true transition kernel with the empirical transition kernel 

ˆPN (·| s, a ) := 1

> N

PNi=1 δΠS ( ˆfN (s,a )+ˆ ϵ(i) 

> N)

(·) supported on the 

N scenarios with equal probability 1/N , where δ(·) denotes the Dirac measure. Based on this empirical transition kernel, we define the corresponding residuals-based Bellman optimality operator as follows 

( ˆTN Q)( s, a ) := c(s, a ) + γ

Z

> S

min  

> a′∈A

Q(s′, a ′) ˆPN (d s′ | s, a )= c(s, a ) + γN

> N

X

> i=1

min  

> a′∈A

Q



ΠS ( ˆfN (s, a ) + ˆ ϵ(i) 

> N

), a ′

. (3) To facilitate analysis, we also introduce the following full-information Bellman optimality operator. Under the true regression f ⋆, we use the residuals ϵ(i) = s′(i) −

f ⋆(s(i), a (i)), i = 1 , . . . , N to approximate the true error ϵ.Correspondingly, we construct the full-information transition kernel as P ⋆N (·| s, a ) := 1

> N

PNi=1 δf ⋆(s,a )+ ϵ(i) (·). Note that because f ⋆(s, a ) + ϵ(i) falls within the state space S, we do not need a projection here. As a result, (1) can be approximated with the full-information Bellman optimality operator as 

(T ⋆N Q)( s, a ) := c(s, a ) + γ

Z

> S

min  

> a′∈A

Q(s′, a ′) P ⋆N (d s′ | s, a )      

> 2See Remark 1 in [13] when f∗̸∈ F . In this misspecified setting, we can replace f∗with the best approximator in the class in our analysis.

= c(s, a ) + γN

> N

X

> i=1

min  

> a′∈A

Q(f ⋆(s, a ) + ϵ(i), a ′). (4) In practice, this full-information Bellman optimality oper-ator cannot be implemented because both the true transition model f ⋆ and residuals ϵ(i) remain unknown. Instead, we will implement the residuals-based Bellman optimality op-erator based on the estimated transition model ˆfN and em-pirical residuals ˆϵN . However, the full-information Bellman optimality operator will be useful in the analysis to bound the gap between T ⋆ and ˆTN as an intermediate step. In the remainder of this section, we study the Lipschitz continuity of the fixed points induced by the full-information Bellman optimality operator T ⋆N and the true Bellman optimality operator T ⋆. We begin by showing that both ˆTN and T ⋆N are contraction mappings in the next theorem, which guarantees the existence and uniqueness of their fixed points. 

Theorem 1: For 0 ≤ γ < 1, the residuals-based Bell-man optimality operator ˆTN defined in (3) and the full-information Bellman operator T ⋆N defined in (4) are γ-contraction mappings, i.e., ∀ Q1, Q 2 ∈ B (S × A ), we have 

∥ ˆTN Q1 − ˆTN Q2∥∞ ≤ γ∥Q1 − Q2∥∞,

∥T ⋆N Q1 − T ⋆N Q2∥∞ ≤ γ∥Q1 − Q2∥∞

Proof: Given a state-action pair (s, a ) ∈ S × A ,based on (3) and denoting s′ 

> i

:= Π S ( ˆfN (s, a ) + ˆ ϵ(i) 

> N

) for 

i = 1 , . . . , N , we have 

( ˆTN Q1)( s, a ) − ( ˆTN Q2)( s, a )

= γN

> N

X

> i=1



min  

> a′∈A

Q1 s′

> i

, a ′ − min  

> a′∈A

Q2 s′

> i

, a ′

≤ γN

> N

X

> i=1

sup 

> a′∈A

Q1(s′

> i

, a ′) − Q2(s′

> i

, a ′)

≤ γ sup 

> (s′,a ′)∈S×A

Q1(s′, a ′) − Q2(s′, a ′) = γ∥Q1 − Q2∥∞.

(5) Taking the supremum over (s, a ) ∈ S × A on both sides yields ∥ ˆTN Q1 − ˆTN Q2∥∞ ≤ γ∥Q1 − Q2∥∞. The proof for the full-information Bellman optimality operator T ⋆N pro-ceeds in the same way as (5), with s′ 

> i

= Π S ( ˆfN (s, a ) + ˆ ϵ(i) 

> N

)

replaced by s′ 

> i

= f ⋆(s, a ) + ϵ(i) based on the definition of 

T ⋆N defined in (4). Since both residuals-based and full-information Bellman optimality operators are contraction mappings, applying them iteratively will lead to their unique fixed points, denoted by 

ˆQN and Q⋆N , respectively. To derive the Lipschitz continuity of Q⋆N , we first make the following assumptions. 

Assumption 1: The immediate cost c(s, a ) is Lipschitz continuous on s ∈ S uniformly over a ∈ A, i.e., 

sup a∈A |c(s1, a ) − c(s2, a )| ≤ Lc∥s1 − s2∥, ∀s1, s 2 ∈ S 

with some Lipschitz constant Lc < +∞.

Assumption 2: The true transition function f ⋆ is Lips-chitz continuous on s ∈ S uniformly over a ∈ A , i.e., 

sup a∈A ∥f ⋆(s1, a )−f ⋆(s2, a )∥ ≤ Lf ∥s1 −s2∥, ∀s1, s 2 ∈ S 

with some Lipschitz constant Lf < +∞.Assumption 2 can be satisfied by many commonly used transition models under a compact action space A.Next, we show that the fixed point of the full-information Bellman optimality operator ( Q⋆N ) is Lipschitz continuous. 

Proposition 1 (Lipschitz continuity of Q⋆N ): Suppose As-sumptions 1 and 2 hold. Consider the full-information Bell-man optimality operator defined in (4). If γL f < 1, then the fixed point Q⋆N (·, a ) is Lipschitz continuous in s uniformly over a ∈ A , i.e., 

sup 

> a∈A

|Q⋆N (s1, a )−Q⋆N (s2, a )| ≤ LQ⋆N ∥s1 −s2∥, ∀s1, s 2 ∈ S ,

where LQ⋆N := Lc 

> 1−γL f

. Moreover, define the value function 

V ⋆N (s) := min a∈A Q⋆N (s, a ). Then V ⋆N (s) is also Lipschitz continuous with the same Lipschitz constant LQ⋆N .

Proof: Fix any initialization Q(1)  

> N

∈ B (S × A ) such that Q(1)  

> N

(·, a ) is Lipschitz continuous in s uniformly over 

a ∈ A , i.e., sup a∈A |Q(1)  

> N

(s1, a ) − Q(1)  

> N

(s2, a )| ≤ L1∥s1 −

s2∥, ∀s1, s 2 ∈ S for some constant L1 < +∞. Define the full-information value-iteration sequence by Q(k+1)  

> N

:= 

T ⋆N Q(k) 

> N

, ∀k ≥ 1. We will prove the Lipschitz continuity of 

Q(k) 

> N

for all k ≥ 1 using mathematical induction. First of all, 

Q(1)  

> N

is Lipschitz continuous with constant L1 according to our initialization. Now suppose that Q(k) 

> N

(·, a ) is Lipschitz continuous in s uniformly over a with a Lipschitz constant 

Lk, i.e., sup a∈A |Q(k) 

> N

(s1, a ) − Q(k) 

> N

(s2, a )| ≤ Lk∥s1 −

s2∥, ∀s1, s 2 ∈ S .Define V (k) 

> N

(s) := min a′∈A Q(k) 

> N

(s, a ′). Then |V (k) 

> N

(s1) −

V (k) 

> N

(s2)| ≤ sup a′∈A |Q(k) 

> N

(s1, a ′) − Q(k) 

> N

(s2, a ′)| ≤

Lk∥s1 − s2∥. As a result, V (k) 

> N

is Lk-Lipschitz continuous. For any a ∈ A and s1, s 2 ∈ S ,

|Q(k+1)  

> N

(s1, a ) − Q(k+1)  

> N

(s2, a )|

= (T ⋆N Q(k) 

> N

)( s1, a ) − (T ⋆N Q(k) 

> N

)( s2, a )

≤| c(s1, a ) − c(s2, a )| + γN

> N

X

> i=1

V (k)

> N

 f ⋆(s1, a ) + ϵ(i)

− V (k)

> N

 f ⋆(s2, a ) + ϵ(i)

> (a)

≤ Lc∥s1 − s2∥ + γN

> N

X

> i=1

Lk f ⋆(s1, a ) − f ⋆(s2, a )

> (b)

≤  Lc + γL f Lk

∥s1 − s2∥,

where (a) holds due to Assumption 1 and the Lipschitz continuity of V (k) 

> N

, and (b) holds due to Assumption 2. Taking the supremum over a ∈ A shows that Q(k+1) 

> N

is (Lc + γL f Lk)-Lipschitz continuous, which yields the recursion Lk+1 = Lc + γL f Lk, ∀k ≥ 1, with the initial value L1 < +∞. Denote α := γL f ∈ [0 , 1) . This recursion yields Lk = αk−1L1 + 1−αk−1 

> 1−α

Lc, ∀k ≥ 2. Therefore, 

lim k→∞ Lk = Lc 

> 1−α

= Lc 

> 1−γL f

= LQ⋆N . Since T ⋆N is a γ-contraction in the sup norm, Q(k) 

> N

converges to the unique fixed point Q⋆N . Therefore, the limit Q⋆N is also Lipschitz continuous with the constant LQ⋆N , i.e., sup a∈A |Q⋆N (s1, a )−

Q⋆N (s2, a )| ≤ Lc 

> 1−γL f

∥s1 − s2∥, ∀s1, s 2 ∈ S .Additionally, |V ⋆N (s1) − V ⋆N (s2)| ≤ sup a∈A |Q⋆N (s1, a ) −

Q⋆N (s2, a )| ≤ Lc 

> 1−γL f

∥s1 − s2∥, ∀s1, s 2 ∈ S , which shows that V ⋆N is Lipschitz continuous with the same constant LQ⋆N .To show the Lipschitz continuity of the fixed point of the true Bellman optimality operator Q⋆, we need an additional assumption on the true transition kernel. 

Assumption 3: There exists Lp < +∞ such that 

sup a∈A W1(P (·| s1, a ), P (·| s2, a )) ≤ LP ∥s1 − s2∥ for all 

s1, s 2 ∈ S , where the 1-Wasserstein distance is defined by 

W1(P (·| s1, a ), P (·| s2, a )) := inf    

> ω∈Ω( P(·| s1,a ),P (·| s2,a ))

E(X,Y )∼ω

∥X − Y ∥.

Here, Ω( μ, ν ) denotes all couplings with marginals μ and ν.

Proposition 2 (Lipschitz continuity of Q⋆): Suppose As-sumptions 1 and 3 hold. Consider the true Bellman optimality operator defined in (1). If γL p < 1, then the fixed point 

Q⋆(·, a ) is Lipschitz continuous in s uniformly over a ∈ A ,i.e., 

sup 

> a∈A

|Q⋆(s1, a ) − Q⋆(s2, a )| ≤ LQ⋆ ∥s1 − s2∥, ∀s1, s 2 ∈ S ,

where LQ⋆ := Lc 

> 1−γL p

. Moreover, define the value function 

V ⋆(s) := min a∈A Q⋆(s, a ). Then V ⋆(s) is also Lipschitz continuous with the same Lipschitz constant LQ⋆ .

Proof: For any given initialization Q(1) ∈ B (S × A )

such that Q(1) (·, a ) is Lipschitz continuous in s uniformly over a ∈ A for some constant L1 < +∞. Define the value-iteration sequence by Q(k+1) := T ⋆Q(k), ∀k ≥ 1. Follow-ing the proof of Proposition 1, we will use mathematical induction to prove that Q(k)(·, a ) is Lipschitz continuous in s uniformly over a ∈ A for all k ≥ 1. First of all, 

Q(1) is Lipschitz continuous with constant L1. Now suppose 

Q(k)(·, a ) is Lipschitz continuous in s uniformly over a

with a Lipschitz constant Lk, i.e., sup a∈A |Q(k)(s1, a ) −

Q(k)(s2, a )| ≤ Lk∥s1 − s2∥, ∀s1, s 2 ∈ S .Define V (k)(s) := min a′∈A Q(k)(s, a ′). Then simi-larly, V (k) is Lipschitz continuous with constant Lk, i.e., 

|V (k)(s1) − V (k)(s2)| ≤ Lk∥s1 − s2∥. For any s1, s 2 ∈ S ,

sup 

> a∈A

|Q(k+1) (s1, a ) − Q(k+1) (s2, a )|

= sup 

> a∈A

(T ⋆Q(k))( s1, a ) − (T ⋆Q(k))( s2, a )

≤ sup 

> a∈A

|c(s1, a ) − c(s2, a )|

+ γ sup 

> a∈A

Z

> S

V (k)(s′) P (d s′|s1, a ) −

Z

> S

V (k)(s′) P (d s′|s2, a )

> (a)

≤ Lc∥s1 − s2∥ + γL k Lp s1 − s2 ,

where (a) follows from Assumptions 1, 3, the Lipschitz continuity of V (k) with constant Lk,and Kantorovich-Rubinstein duality for the 1-Wasserstein distance [14]: W1

 P (·| s1, a ), P (·| s2, a ) =sup Lip (f )≤1{R f (s′)P (ds ′|s1, a ) − R f (s′)P (ds ′|s2, a )}.The remaining steps follow directly from the proof of Proposition 1. In particular, we obtain the recursion 

Lk+1 = Lc + γL pLk, k ≥ 1, which yields lim k→∞ Lk =Lc 

> 1−γL p

= LQ⋆ . Since T ⋆ is a γ-contraction in the sup norm, Q(k) converges to the unique fixed point Q⋆, which is Lipschitz continuous with constant LQ⋆ . Additionally, 

|V ⋆(s1) − V ⋆(s2)| ≤ sup a∈A |Q⋆(s1, a ) − Q⋆(s2, a )| ≤ 

> Lc
> 1−γL p

∥s1 − s2∥, ∀s1, s 2 ∈ S , which shows that V ⋆ is Lipschitz continuous with the same constant LQ⋆ .

B. Consistency and Finite Sample Guarantees 

In this section, we establish the consistency and finite sam-ple guarantees of the residuals-based offline RL framework. In particular, we identify conditions under which (i) the fixed point ˆQN of the residuals-based Bellman optimality operator converges to the fixed point Q⋆ of the true Bellman operator asymptotically and (ii) the probability of the error ˆQN − Q⋆

exceeding a prescribed tolerance can be upper bounded. We first introduce the following assumptions on the regression estimate. 

Assumption 4: The regression estimate has the following uniform consistency property, i.e., 

sup 

> (s,a )∈S×A

∥ ˆfN (s, a ) − f ⋆(s, a )∥ p

−→ 0, (6) 

1

N

> N

X

> i=1

∥ ˆfN (s(i), a (i)) − f ⋆(s(i), a (i))∥ p

−→ 0 (7) The uniform convergence of ˆfN (s, a ) over S × A in Assumption 4 is satisfied by a wide range of regression methods under the compactness of S and A, e.g., see detailed discussions under Assumption 9 in [15] for linear regression and Assumption 2 in [16] for hybrid linear-exponential regression. If we assume both S and A are compact, and the regression estimator is weakly consistent, then one can obtain the uniform consistency in (6). 

Assumption 5: The following uniform weak law of large numbers (LLN) holds: 

sup 

> s,a

1

N

> N

X

> i=1

V ⋆ f ⋆(s, a ) + ϵ(i) − Eϵ

V ⋆ f ⋆(s, a ) + ϵ p

−→ 0.

(8) 

Assumption 5 can be interpreted as the uniform weak LLN on the mean of the value function. Based on Theorem 7.48 of [17], Assumption 5 holds under the following conditions: 1) V ⋆ f ⋆(s, a ) + ϵ is continuous on (s, a ) ∈ S × A ;2) {| V ⋆ f ⋆(s, a ) + ϵ| : ( s, a ) ∈ S × A} is dominated by an integrable function; 3) the samples ϵ(i) are i.i.d.; 4) Both S and A are compact. Similar conditions also exist under non i.i.d. settings. 

Theorem 2 (Consistency of fixed point ˆQN ): Suppose Assumptions 1-5 hold. The fixed point ˆQN of the residuals-based Bellman optimality operator is asymptotically optimal, i.e., ∥ ˆQN − Q⋆∥∞

> p

−→ 0 as N → ∞ .

Proof: According to the contractive property of T ⋆ and 

ˆTN , we have Q⋆ = T ⋆Q⋆, ˆQN = ˆTN ˆQN and 

∥ ˆQN − Q⋆∥∞ = ∥ ˆTN ˆQN − T ⋆Q⋆∥∞

≤ ∥ ˆTN ˆQN − ˆTN Q⋆∥∞ + ∥ ˆTN Q⋆ − T ⋆Q⋆∥∞

≤ γ∥ ˆQN − Q⋆∥∞ + ∥( ˆTN − T ⋆)Q⋆∥∞.

Therefore, 

∥ ˆQN − Q⋆∥∞ ≤ 11 − γ ∥( ˆTN − T ⋆)Q⋆∥∞. (9) Next, we bound the right-hand side of (9). Given a state-action pair (s, a ) ∈ S ×A , according to the definitions of T ⋆

and ˆTN and denoting V ⋆(s) = min a∈A Q⋆(s, a ), we have                                                   

> (( ˆTN−T⋆)Q⋆)( s, a )
> =γN
> NX
> i=1
> V⋆(Π S( ˆfN(s, a ) + ˆ ϵ(i)
> N)) −Eϵ[V⋆(f⋆(s, a ) + ϵ)]
> ≤γN
> NX
> i=1
> 
> V⋆ ΠS( ˆfN(s, a ) + ˆ ϵ(i)
> N)−V⋆ ΠS(f⋆(s, a ) + ˆ ϵ(i)
> N)|{z }
> (i)
> +γN
> NX
> i=1
> 
> V⋆ ΠS(f⋆(s, a ) + ˆ ϵ(i)
> N)−V⋆ f⋆(s, a ) + ϵ(i)|{z }
> (ii )
> +γN
> NX
> i=1
> V⋆ f⋆(s, a ) + ϵ(i)−Eϵ
> V⋆ f⋆(s, a ) + ϵ |{z }
> (iii )
> .

For (i), we have 

γN

> N

X

> i=1



V ⋆ ΠS ( ˆfN (s, a ) + ˆ ϵ(i) 

> N

) − V ⋆ ΠS (f ⋆(s, a ) + ˆ ϵ(i) 

> N

)

> (a)

≤ γN

> N

X

> i=1

LQ⋆ ΠS ( ˆfN (s, a ) + ˆ ϵ(i) 

> N

) − ΠS (f ⋆(s, a ) + ˆ ϵ(i) 

> N

)

> (b)

≤ γL Q⋆ ∥ ˆfN (s, a ) − f ⋆(s, a )∥,

where (a) holds by Proposition 2 and (b) holds by the Lipschitz continuity of the orthogonal projection. Taking the supremum over (s, a ) on both sides and driving N to +∞,

(i) converges to 0 in probability due to (6) in Assumption 4. For (ii ), we first derive 

ˆϵ(i) 

> N

− ϵ(i) =  s′(i) − ˆfN (s(i), a (i)) −  s′(i) − f ⋆(s(i), a (i))

= f ⋆(s(i), a (i)) − ˆfN (s(i), a (i)). (10) Then we have 

γN

> N

X

> i=1



V ⋆ ΠS (f ⋆(s, a ) + ˆ ϵ(i) 

> N

) − V ⋆ f ⋆(s, a ) + ϵ(i)

> (a)

≤ γN

> N

X

> i=1

LQ⋆

 f ⋆(s, a ) + ˆ ϵ(i)

> N

 −  f ⋆(s, a ) + ϵ(i)

> (b)

≤ γN

> N

X

> i=1

LQ⋆ f ⋆(s(i), a (i)) − ˆfN (s(i), a (i))

where (a) is due to Proposition 2 and the Lipschitz continuity of the orthogonal projection and (b) is due to (10). Taking supremum over (s, a ) on both sides and driving N to +∞,

(ii ) converges to 0 due to (7) in Assumption 4. Finally, taking the supremum over (s, a ), (iii ) converges to 0 in probability due to Assumption 5. Overall, this completes the proof. 

Corollary 1 (Consistency of the value function ˆVN ): 

Suppose Assumptions 1-5 hold. Define the value function 

ˆVN (s) = min a∈A ˆQN (s, a ). Then, ∥ ˆVN − V ⋆∥∞

> p

−→ 0 as N → ∞ .

Proof: From the definitions of V ⋆ and 

ˆVN , we have sup s∈S | ˆVN (s) − V ⋆(s)| =sup s∈S min a∈A ˆQN (s, a ) − min a∈A Q⋆(s, a ) ≤

sup s∈S ,a ∈A ˆQN (s, a ) − Q⋆(s, a ) p

−→ 0 as N → ∞.To develop the finite sample guarantee for ˆQN , we make the following uniform exponential bound assumption on the full-information Q-function Q⋆N and the finite-sample assumption on the regression estimate. 

Assumption 6: The full-information fixed point possess the following uniform exponential bound: for any constant 

η, there exist positive constants A(η) and α(η) such that 

P

(

sup 

> (s,a )∈S×A

|Q⋆N (s, a ) − Q⋆(s, a )| > η 

)

≤ A(η)e−N α (η).

Assumption 7: The regression estimate ˆfN possess the following finite sample properties: for any constant 

η > 0 and N ∈ N, there exist positive con-stants Af (η), ¯Af (η), αf (N, η ), and ¯αf (N, η ) with 

lim N →∞ αf (N, η ) = ∞, lim N →∞ ¯αf (N, η ) = ∞ such that                  

> P
> (
> sup
> (s,a )∈S×A
> ∥f⋆(s, a )−ˆfN(s, a )∥> η
> )
> ≤Af(η)e−αf(N,η ),
> P
> (
> 1
> N
> NX
> i=1
> ∥f⋆(s(i), a (i))−ˆfN(s(i), a (i))∥> η
> )
> ≤¯Af(η)e−¯αf(N,η ).

Assumption 7 can be satisfied by a wide range of paramet-ric and nonparametric regression methods if we assume both 

S and A are compact. See more discussions in Appendix F in [13] and Assumption 6 in [15]. Next, we define the discrepancy in the i-th scenario between the residuals-based and full-information prediction as eϵ(i) 

> N

(s, a ) := ( ˆfN (s, a ) + ˆ ϵ(i) 

> N

) − (f ⋆(s, a ) + ϵ(i)). We show that the distance between ˆQN and Q⋆N can be upper bounded by the norm of eϵ(i) 

> N

(s, a ) in the next lemma. 

Lemma 1: Under Assumptions 1 and 2, the following inequality holds: 

ˆQN − Q⋆N ∞

≤ γL Q⋆N

1 − γ sup 

> (s,a )∈S×A

1

N

> N

X

> i=1

∥eϵ(i) 

> N

(s, a )∥

!

.

(11) 

Proof: Recall that ˆQN , Q⋆N are the fixed points of the residuals-based ( ˆTN ) and full-information ( T ⋆N ) Bellman optimality operator, i.e., ˆQN = ˆTN ( ˆQN ), Q⋆N = T ⋆N (Q⋆N ).Following the same proof for (9) and denoting V ⋆N (s) = min a∈A Q⋆N (s, a ), we have                                            

> ˆQN−Q⋆N∞≤11−γ( ˆTN−T⋆N)Q⋆N∞
> ≤γ
> 1−γsup
> s,a
> 1
> N
> NX
> i=1
> V⋆N
>  ΠS( ˆfN(s, a ) + ˆ ϵ(i)
> N)−V⋆N
>  f⋆(s, a ) + ϵ(i)
> (a)
> ≤γL Q⋆N
> 1−γsup
> s,a
> 1
> N
> NX
> i=1
> ΠS
>  ˆfN(s, a ) + ˆ ϵ(i)
> N
> − f⋆(s, a ) + ϵ(i)
> (b)
> ≤γL Q⋆N
> 1−γsup
> s,a
> 1
> N
> NX
> i=1
> eϵ(i)
> N(s, a ),(12)

where (a) holds due to Proposition 1 and (b) holds due to the Lipschitz continuity of the orthogonal projection. 

Lemma 2: Suppose Assumptions 1, 2, and 7 hold. Then for any constant η > 0 and N ∈ N, there exist positive constants ¯A(η) and ¯α(N, η ) with lim N →∞ ¯α(N, η ) = ∞

such that 

P

(

sup 

> (s,a )∈S×A

| ˆQN (s, a ) − Q⋆N (s, a )| > η 

)

≤ ¯A(η)e− ¯α(N,η ).

Proof: Lemma 1 implies 

P

(

sup 

> (s,a )∈S×A

| ˆQN (s, a ) − Q⋆N (s, a )| > η 

)

≤P

( γL Q⋆N

1 − γ sup 

> (s,a )∈S×A

 1

N

> N

X

> i=1

∥eϵ(i) 

> N

(s, a )∥



> η 

)

≤P

(

sup 

> (s,a )∈S×A

 1

N

> N

X

> i=1

∥eϵ(i) 

> N

(s, a )∥



> η(1 − γ)

γL Q⋆

)

Then, with a change of variable, it suffices to show for any 

η > 0 and N ∈ N, there exist some constants ¯A(η) > 0 and 

¯α(N, η ) > 0 such that 

P

(

sup 

> (s,a )∈S×A

 1

N

> N

X

> i=1

∥eϵ(i) 

> N

(s, a )∥



> η 

)

≤ ¯A(η)e− ¯α(N,η ).

Recall that 1

> N

PNi=1 ∥eϵ(i) 

> N

(s, a )∥ = 1

> N

PNi=1 ∥( ˆfN (s, a ) + ˆϵ(i) 

> N

) − (f ⋆(s, a ) + ϵ(i))∥ ≤ ∥ ˆfN (s, a ) − f ⋆(s, a )∥ +

> 1
> N

PNi=1 ∥ˆϵ(i) 

> N

− ϵ(i)∥ = ∥ ˆfN (s, a ) − f ⋆(s, a )∥ +

> 1
> N

PNi=1 ∥f ⋆(s(i), a (i))− ˆfN (s(i), a (i))∥ where the last equal-ity is due to (10). Consequently, 

P

(

sup 

> (s,a )∈S×A

 1

N

> N

X

> i=1

∥eϵ(i) 

> N

(s, a )∥



> η 

)

≤P

(

sup 

> (s,a )∈S×A

∥ ˆfN (s, a ) − f ⋆(s, a )∥ > η/ 2

)

+ P

(

1

N

> N

X

> i=1

∥ ˆfN (s(i), a (i)) − f ⋆(s(i), a (i))∥ > η/ 2

)

> (a)

≤ Af ( η

2 )e−αf (N, η  

> 2)

+ ¯Af ( η

2 )e− ¯αf (N, η 

> 2)

≤



Af

 η

2



+ ¯Af

 η

2

 

e− min (αf(N, η 

> 2

), ¯αf(N, η 

> 2

)) = ¯A(η)e− ¯α(N,η )

where (a) is due to Assumption 7 and ¯α(N, η ) := min {αf

 N, η

> 2

 , ¯αf

 N, η

> 2

} converges to 0 as N → ∞

according to Assumption 7. 

Theorem 3 (Finite sample guarantee of ˆQN ): Suppose Assumptions 1, 2, 6 and 7 hold. Then for any constant 

η > 0 and N ∈ N, there exist positive constants Γ( η) and 

γ(N, η ) with lim N →∞ γ(N, η ) = ∞ such that the fixed points ˆQN and Q⋆ satisfy 

P

n

∥ ˆQN − Q⋆∥∞ > η 

o

≤ Γ( η) exp ( −γ(N, η )) .

Proof: For any η > 0, we have 

P

n

∥ ˆQN − Q⋆∥∞ > η 

o≤P

n

∥ ˆQN − Q⋆N ∥∞ > η

> 2

o

+ P∥Q⋆N − Q⋆∥∞ > η

> 2
> (a)

≤ ¯A  η

> 2

 exp( −¯α  N, η

> 2

) + A  η

> 2

 exp( −N α   η

> 2

),

where (a) is due to Lemma 2 and Assumption 6. Define Γ( η) := ¯A  η

> 2

 + A  η

> 2

 , γ(N, η ) := min ¯α N, η

> 2

 , N α   η

> 2

 . As a result, we have 

P

n

∥ ˆQN − Q⋆∥∞ > η 

o

≤ Γ( η) exp( −γ(N, η )) , which completes the proof. 

Corollary 2 (Finite sample guarantee of ˆVN ): Suppose Assumptions 1, 2, 6 and 7 hold. Define the value function 

ˆVN (s) = min a∈A ˆQN (s, a ). Then for any constant η > 0

and N ∈ N, there exist positive constants Γ( η) and γ(N, η )

such that P

n

∥ ˆVN − V ⋆∥∞ > η 

o

≤ Γ( η) exp  −γ(N, η ).Theorem 3 and Corollary 2 illustrate that the probability of the optimality gap of the value function exceeding a prescribed tolerance decays exponentially with respect to 

γ(N, η ).

C. Residuals-based Offline Deep Q-Learning 

While Section III-B establishes the consistency and finite sample guarantees when we can evaluate the Q-function exactly, in this section, we introduce a practical residuals-based deep Q-learning (DQN) algorithm in Algorithm 1 for continuous state and discrete action spaces. Compared to the traditional DQN algorithm discussed in [18] and [19], our approach first fits a regression model ˆfN using the offline dataset DN and then computes the corresponding empirical residuals. These residuals are used to construct a simulated transition kernel, where the next state st+1 

is generated by adding a sampled residual to the model prediction ˆfN (st, a t). Specifically, starting from any state-action pair st, a t, we sample an empirical residual ˆϵt from the residual set {ˆϵ(i) 

> N

}Ni=1 and construct the next state as 

st+1 = Π S ( ˆfN (s, a ) + ˆ ϵt). We then train our policy using the generated trajectories. 

Algorithm 1 Residuals-based Offline Deep Q-Learning. 

1: Input: Dataset DN = {(s(i), a (i), c (i), s ′(i)}Ni=1 .

2: Initialize replay memory D.

3: Fit a regression model ˆfN using dataset DN and calculate the empirical residuals {ˆϵ(i)          

> N}Ni=1 , where ˆϵ(i)
> N=s′(i)−ˆfN(s(i), a (i)),∀i= 1 , . . . , N .

4: Initialize action value function Q with random weights θ.

5: Initialize target action value function ˆQ with random weights ˆθ.

6: for episode k = 1 , 2, . . . do 

7: Initialize state s1.

8: for time step t = 1 , 2, . . . do 

9: With probability ϵ select a random action at ∈ A .

10: Otherwise select at = arg min a′ Q(st, a ′; θ).

11: Sample an empirical residual ˆϵt from the set {ˆϵ(i)          

> N}Ni=1 and update next state as st+1 = Π S( ˆfN(st, a t) + ˆ ϵt)and cost as ct=c(st, a t).

12: Store transition (st, a t, c t, s t+1 ) in replay memory D.

13: Sample random minibatch of M transitions from D:   

> {(sj, a j, c j, s j+1 )}Mj=1 .

14: Set yj = cj + γ min a′ ˆQ(sj+1 , , a ′; ˆθ) for all j = 1 , . . . , M .

15: Calculate loss L = 1     

> M
> PMj=1 (yj−Q(sj, a j;θ)) 2.

16: Perform a gradient descent step to minimize the loss L with respect to the network parameter θ.

17: Every C steps reset ˆQ = Q.

18: end for 

19: end for 

IV. N UMERICAL RESULTS 

We implement Algorithm 1 in a stochastic variant of the CartPole environment, where we add a Gaussian noise 

ϵ ∼ N (0 , 0.25) to the observed cart position to enable stochastic transitions. We first collect transition data from this true environment and fit a neural network that takes the current state-action pair as input and predicts the next state, where we consider a single hidden layer with 64 neurons and ReLU activation. The Q-network is built as a fully connected neural network with two hidden layers of width 64 and ReLU activations. We train the Q-network within the simulated transition kernel using the Adam optimizer, with learning rate 

10 −4, and discount factor γ = 0 .99 . We adopt an ϵ-greedy action selection rule, where ϵ is initialized at 0.9 and decays exponentially to reach a minimum value of 0.01. For each run, the policy is trained for 1000 episodes in the residuals-based simulator and tested in the true environment. We report the average results over 5 independent runs. 

A. Comparison of Different Sample Sizes 

We evaluate the proposed residuals-based offline DQN algorithm under different number of trajectories sampled in the offline dataset, denoted by n. Specifically, we consider 

n ∈ { 500 , 600 , 700 , 800 , 900 , 1000 }. We report the average training and testing rewards from 5 independent runs in Fig. 2. From Fig. 2, both the training and testing rewards increase 

(a) Training 

(b) Testing 

Fig. 2: Comparison of different sample sizes as n increases, which indicates that increasing the amount of offline data improves the quality of the learned transition model and the effectiveness of the resulting policy in the testing environment. B. Comparison of Different Models 

We also compare our model with a baseline approach in which the next state is predicted only using the regression 

ˆfN (s, a ), without adding the empirical residuals. Fig. 3 presents both the training and testing performances for our model (w/ residuals) and the benchmark (w/o residuals), where we take n = 500 and n = 1000 .

(a) Training 

(b) Testing 

Fig. 3: Comparison of Models Fig. 3 shows that even if the baseline model achieves better training performance, it performs poorly in the true environment for both n = 500 and n = 1000 . On the other hand, our residuals-based approach always outperforms by achieving a higher testing reward in the true environment. Specifically, when n = 1000 , the training and testing performances under the residuals-based approach are similar, which shows that it provides an accurate approximation of the underlying stochastic transition. This illustrates the benefit of accounting for estimation error in predicting the transition dynamics in offline RL. V. CONCLUSIONS In this paper, we introduced a residuals-based offline RL framework that directly links the estimation error in learning transition dynamics to downstream policy optimization. In particular, we first fit a regression model and constructed empirical residuals based on the offline dataset. Then, we defined a residuals-based Bellman optimality operator, where the transition kernel is approximated by the estimated re-gression model and empirical residuals. We proved that this residuals-based Bellman optimality operator is a contraction mapping and established consistency and finite-sample guar-antees for its corresponding fixed point. For practical use, we also designed a residuals-based offline DQN algorithm and demonstrated its superior performance through numerical experiments on a stochastic CartPole environment. As a possible future direction, we will extend the proposed frame-work to an empirical residuals-based distributionally robust optimization setting for offline RL, which will account for distributional ambiguity and improve robustness for learning the uncertain transition dynamics. VI. ACKNOWLEDGMENTS This work was supported in part by the National Science Foundation under Grant #2331782. REFERENCES [1] M. L. Puterman, Markov Decision Processes: Discrete Stochastic Dynamic Programming . John Wiley & Sons, 2014. [2] R. S. Sutton and A. G. Barto, Reinforcement Learning: An Introduc-tion . MIT Press, 2018. [3] S. Levine, A. Kumar, G. Tucker, and J. Fu, “Offline reinforcement learning: Tutorial, review, and perspectives on open problems,” arXiv preprint arXiv:2005.01643 , 2020. [4] R. Munos, “Error bounds for approximate value iteration,” in Pro-ceedings of the National Conference on Artificial Intelligence , vol. 20, no. 2. Menlo Park, CA; Cambridge, MA; London; AAAI Press; MIT Press; 1999, 2005, p. 1006. [5] R. Munos and C. Szepesv´ ari, “Finite-time bounds for fitted value iteration.” Journal of Machine Learning Research , vol. 9, no. 5, 2008. [6] J. Chen and N. Jiang, “Information-theoretic considerations in batch reinforcement learning,” in International Conference on Machine Learning . PMLR, 2019, pp. 1042–1051. [7] N. Jiang and T. Xie, “Offline reinforcement learning in large state spaces: Algorithms and guarantees,” Statistical Science , vol. 40, no. 4, pp. 570–596, 2025. [8] S. Kakade and J. Langford, “Approximately optimal approximate reinforcement learning,” in International Conference on Machine Learning . PMLR, 2002, pp. 267–274. [9] J. Schulman, S. Levine, P. Abbeel, M. Jordan, and P. Moritz, “Trust region policy optimization,” in International Conference on Machine Learning . PMLR, 2015, pp. 1889–1897. [10] S. Fujimoto, D. Meger, and D. Precup, “Off-policy deep reinforcement learning without exploration,” in International conference on machine learning . PMLR, 2019, pp. 2052–2062. [11] A. Kumar, J. Fu, M. Soh, G. Tucker, and S. Levine, “Stabilizing off-policy Q-learning via bootstrapping error reduction,” Advances in Neural Information Processing Systems , vol. 32, 2019. [12] Y. Wu, G. Tucker, and O. Nachum, “Behavior regularized offline reinforcement learning,” arXiv preprint arXiv:1911.11361 , 2019. [13] R. Kannan, G. Bayraksan, and J. R. Luedtke, “Data-driven sample average approximation with covariate information,” Operations Re-search , 2025. [14] C. Villani, Topics in optimal transportation . American Mathematical Soc., 2021, vol. 58. [15] Q. Zhu, X. Yu, and G. Bayraksan, “Residuals-based contextual distri-butionally robust optimization with decision-dependent uncertainty,” 

arXiv preprint arXiv:2406.20004 , 2024. [16] H. Sun, X. Yu, and G. Bayraksan, “Contextual stochastic optimiza-tion for determining electric vehicle charging station locations with decision-dependent demand learning,” Available at SSRN 5078715 ,2025. [17] A. Shapiro, D. Dentcheva, and A. Ruszczynski, Lectures on Stochastic Programming: Modeling and Theory . SIAM, 2021. [18] V. Mnih, K. Kavukcuoglu, D. Silver, A. Graves, I. Antonoglou, D. Wierstra, and M. Riedmiller, “Playing atari with deep reinforcement learning,” arXiv preprint arXiv:1312.5602 , 2013. [19] V. Mnih, K. Kavukcuoglu, D. Silver, A. A. Rusu, J. Veness, M. G. Bellemare, A. Graves, M. Riedmiller, A. K. Fidjeland, G. Ostrovski 

et al. , “Human-level control through deep reinforcement learning,” 

Nature , vol. 518, no. 7540, pp. 529–533, 2015.