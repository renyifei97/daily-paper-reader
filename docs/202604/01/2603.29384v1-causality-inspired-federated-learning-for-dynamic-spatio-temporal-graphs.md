---
title: Causality-inspired Federated Learning for Dynamic Spatio-Temporal Graphs
authors: "Yuxuan Liu, Wenchao Xu, Haozhao Wang, Zhiming He, Zhaofeng Shi, Chongyang Xu, Peichao Wang, Boyuan Zhang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29384v1"
tags: ["query:urt-pf"]
score: 8.0
evidence: 动态时空图上的联邦学习
tldr: 针对动态时空图联邦学习中存在的空间和时间异质性问题，本文提出了一种受因果启发的框架 SC-FSGL。该方法通过表示层面的干预，将可迁移的因果知识与客户端特定的噪声解耦。通过引入条件分离模块和因果代码簿，实现了不变时空因果因子的提取和跨客户端的一致性对齐，有效解决了表示纠缠和负迁移问题，在多个异质性时空图数据集上取得了领先性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29384v1/fig-001.webp\", \"caption\": \"Figure 5: Ablation Study of SC-FSGL.\", \"page\": 7, \"index\": 1, \"width\": 501, \"height\": 346}]"
motivation: 现有联邦图学习方法因忽略时空异质性而导致虚假表示纠缠和负迁移，限制了模型在动态环境下的泛化表现。
method: 提出 SC-FSGL 框架，通过条件分离模块模拟软干预以解耦因果因子，并利用因果代码簿进行对比学习以对齐局部表示。
result: 在五个具有显著异质性的时空图数据集上，SC-FSGL 的表现均优于当前最先进的联邦学习方法。
conclusion: 通过因果启发的方法显式解耦可迁移知识与客户端特定噪声，能有效提升动态时空图联邦学习的鲁棒性和泛化能力。
---

## Abstract
Federated Graph Learning (FGL) has emerged as a powerful paradigm for decentralized training of graph neural networks while preserving data privacy. However, existing FGL methods are predominantly designed for static graphs and rely on parameter averaging or distribution alignment, which implicitly assume that all features are equally transferable across clients, overlooking both the spatial and temporal heterogeneity and the presence of client-specific knowledge in real-world graphs. In this work, we identify that such assumptions create a vicious cycle of spurious representation entanglement, client-specific interference, and negative transfer, degrading generalization performance in Federated Learning over Dynamic Spatio-Temporal Graphs (FSTG). To address this issue, we propose a novel causality-inspired framework named SC-FSGL, which explicitly decouples transferable causal knowledge from client-specific noise through representation-level interventions. Specifically, we introduce a Conditional Separation Module that simulates soft interventions through client conditioned masks, enabling the disentanglement of invariant spatio-temporal causal factors from spurious signals and mitigating representation entanglement caused by client heterogeneity. In addition, we propose a Causal Codebook that clusters causal prototypes and aligns local representations via contrastive learning, promoting cross-client consistency and facilitating knowledge sharing across diverse spatio-temporal patterns. Experiments on five diverse heterogeneity Spatio-Temporal Graph (STG) datasets show that SC-FSGL outperforms state-of-the-art methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SC-FSGL**（Shared Causality-inspired Federated Spatio-Temporal Graph Learning）的框架，旨在解决动态时空图（FSTG）在联邦学习环境下的异质性挑战。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的联邦图学习（FGL）方法大多针对静态图设计，且依赖于参数平均或分布对齐。在处理**动态时空图（FSTG）**时，这些方法忽略了客户端之间显著的**空间结构异质性**和**时间动态非平稳性**。
*   **研究动机**：作者发现，简单地假设所有特征在客户端间均可迁移会导致“恶性循环”：虚假表示纠缠、客户端特定信号的干扰以及负迁移。这使得全局模型难以泛化，局部模型难以有效个性化。
*   **整体含义**：受因果推理启发，论文提出将知识划分为“共享因果知识”（跨客户端稳定的模式）和“客户端特定知识”。通过显式解耦这两者，可以提升模型在异质环境下的鲁棒性和预测准确性。

### 2. 方法论：核心思想与关键技术
SC-FSGL 框架的核心思想是通过表示层面的干预来解耦因果因子。
*   **条件分离模块（Conditional Separation Module）**：
    *   利用学习到的**软掩码（Soft Mask）**在潜空间模拟因果干预（do-operator）。
    *   通过掩码将时空特征分解为：**共享因果特征**（用于全局聚合）和**客户端特定特征**（保留在本地）。
*   **因果代码簿（Causal Codebook）**：
    *   在服务器端维护一个可学习的原型矩阵（Codebook），作为语义锚点。
    *   客户端通过**对比学习（Contrastive Learning）**将局部提取的共享因果特征与代码簿中的原型对齐，确保跨客户端的语义一致性。
*   **不变风险最小化（IRM）正则化**：
    *   引入 IRM 惩罚项，强制预测器在不同客户端（环境）下保持最优，从而进一步提取跨环境的不变因果特征。
*   **损失函数组合**：总损失 $L_{local}$ 由预测损失（MSE）、对比对齐损失（$L_{com}$）和 IRM 梯度惩罚项（$L_{irm}$）加权组成。

### 3. 实验设计
*   **数据集**：使用了 5 个真实的交通流量/速度数据集：**METRLA、PEMSD4、PEMSD7(M)、PEMSD8、PEMSBAY**。每个数据集代表一个独立的联邦客户端，具有不同的路网结构和时间分布。
*   **Benchmark 与对比方法**：
    *   **通用联邦学习**：FedAvg, FedProx, Moon, FedRep。
    *   **图联邦学习**：FedStar。
    *   **时空预测模型**：GMAN, MegaCRN。
    *   **最新联邦时空模型**：FUELS。
    *   **基准线**：Local（仅本地训练，不进行联邦聚合）。
*   **评估指标**：MAE（平均绝对误差）、RMSE（均方根误差）、MAPE（平均绝对百分比误差）。

### 4. 资源与算力
*   **硬件环境**：实验在两块 **NVIDIA 3090 GPU** 上运行。
*   **软件框架**：基于 PyTorch 实现。
*   **训练细节**：通信轮数设置了早停机制（Early Stopping），每轮本地训练仅执行 1 个 Epoch。
*   **通信效率**：论文特别提到，SC-FSGL 仅交换轻量级的因果代码簿（约 8KB），而非整个模型参数，显著降低了带宽成本。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个数据集上进行了多步长预测（15/30/60 分钟）的全面对比。
    *   进行了详细的**消融实验**（验证代码簿和分离模块的有效性）。
    *   提供了 **t-SNE 可视化**，展示了因果表示的解耦过程。
    *   包含**收敛性分析**和**超参数敏感性实验**（针对损失权重 $a$ 和 $b$）。
*   **充分性评价**：实验设计非常充分且客观。通过跨多个异质数据集的对比，证明了方法在处理非 IID 数据时的优越性。收敛性证明（Theorem 1）也为实验结果提供了理论支撑。

### 6. 主要结论与发现
*   **性能领先**：SC-FSGL 在所有数据集和所有预测步长上均一致优于 SOTA 方法，尤其是在处理具有强烈异质性的 PEMSD4 和 PEMSD8 时提升显著。
*   **解耦有效性**：t-SNE 可视化证明，模型成功将共享因果特征聚类，同时让私有特征保持发散，有效抑制了负迁移。
*   **通信优势**：通过交换代码簿而非全模型参数，在保持高精度的同时实现了极高的通信效率。

### 7. 优点（亮点）
*   **因果视角**：将因果干预引入联邦时空图学习，从根本上解释并解决了异质性导致的表示纠缠问题。
*   **软干预机制**：无需显式构建复杂的结构因果模型（SCM），通过掩码机制实现了实用的因果解耦。
*   **隐私与效率并重**：代码簿机制不仅保护了模型结构隐私，还极大地压缩了通信量。

### 8. 不足与局限
*   **领域局限性**：实验主要集中在交通预测领域，虽然交通数据具有代表性，但在环境监测、智能电网等其他时空图场景下的泛化能力尚待验证。
*   **长期漂移问题**：论文提到目前尚未显式建模“时间因果漂移”，在极长期的动态环境下，因果模式可能会发生根本性改变，这超出了当前框架的处理范围。
*   **代码簿尺寸**：代码簿的大小（原型数量）目前是预设的固定值，未来若能实现自适应调整会更具灵活性。

（完）
