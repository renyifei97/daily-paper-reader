---
title: Causality-inspired Federated Learning for Dynamic Spatio-Temporal Graphs
title_zh: 受因果启发的动态时空图联邦学习
authors: "Yuxuan Liu, Wenchao Xu, Haozhao Wang, Zhiming He, Zhaofeng Shi, Chongyang Xu, Peichao Wang, Boyuan Zhang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29384v1"
tags: ["query:urt-pf"]
score: 8.0
evidence: 去中心化环境下的动态时空图联邦学习
tldr: 针对动态时空图联邦学习中存在的时空异构性及虚假表示纠缠问题，本文提出了一种受因果启发的框架SC-FSGL。该框架通过条件分离模块解耦不变的因果因素与客户端特定噪声，并利用因果代码簿通过对比学习促进跨客户端的知识共享，有效解决了异构环境下的负迁移问题，提升了模型的泛化性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29384v1/fig-001.webp\", \"caption\": \"Figure 5: Ablation Study of SC-FSGL.\", \"page\": 7, \"index\": 1, \"width\": 501, \"height\": 346}]"
motivation: 现有联邦图学习方法忽略了时空异构性，导致虚假特征纠缠和客户端干扰，限制了模型在动态时空图上的泛化能力。
method: 提出SC-FSGL框架，利用条件分离模块进行表示级干预以解耦因果特征，并结合因果代码簿与对比学习实现跨客户端的知识对齐。
result: 在五个具有显著异构性的时空图数据集上的实验结果表明，SC-FSGL的性能全面优于现有的最先进联邦学习方法。
conclusion: 通过因果干预显式解耦不变因果知识与客户端特定噪声，是提升动态时空图联邦学习鲁棒性和一致性的有效方案。
---

## 摘要
联邦图学习 (FGL) 已成为一种在保护数据隐私的同时进行图神经网络去中心化训练的强大范式。然而，现有的 FGL 方法主要针对静态图设计，并依赖于参数平均或分布对齐，这隐含地假设所有特征在客户端之间具有同等的可迁移性，忽略了现实世界图中存在的时空异质性以及客户端特定知识。在这项工作中，我们发现此类假设会导致伪表示纠缠、客户端特定干扰和负迁移的恶性循环，从而降低了动态时空图联邦学习 (FSTG) 的泛化性能。为了解决这一问题，我们提出了一种名为 SC-FSGL 的新型因果启发框架，该框架通过表示层面的干预，显式地将可迁移的因果知识与客户端特定的噪声解耦。具体而言，我们引入了一个条件分离模块，通过客户端条件掩码模拟软干预，从而实现不变时空因果因素与伪信号的解离，并减轻由客户端异质性引起的表示纠缠。此外，我们提出了一个因果码本 (Causal Codebook)，通过聚类因果原型并通过对比学习对齐局部表示，从而促进跨客户端的一致性，并推动不同时空模式下的知识共享。在五个具有不同异质性的时空图 (STG) 数据集上的实验表明，SC-FSGL 优于现有最先进的方法。

## Abstract
Federated Graph Learning (FGL) has emerged as a powerful paradigm for decentralized training of graph neural networks while preserving data privacy. However, existing FGL methods are predominantly designed for static graphs and rely on parameter averaging or distribution alignment, which implicitly assume that all features are equally transferable across clients, overlooking both the spatial and temporal heterogeneity and the presence of client-specific knowledge in real-world graphs. In this work, we identify that such assumptions create a vicious cycle of spurious representation entanglement, client-specific interference, and negative transfer, degrading generalization performance in Federated Learning over Dynamic Spatio-Temporal Graphs (FSTG). To address this issue, we propose a novel causality-inspired framework named SC-FSGL, which explicitly decouples transferable causal knowledge from client-specific noise through representation-level interventions. Specifically, we introduce a Conditional Separation Module that simulates soft interventions through client conditioned masks, enabling the disentanglement of invariant spatio-temporal causal factors from spurious signals and mitigating representation entanglement caused by client heterogeneity. In addition, we propose a Causal Codebook that clusters causal prototypes and aligns local representations via contrastive learning, promoting cross-client consistency and facilitating knowledge sharing across diverse spatio-temporal patterns. Experiments on five diverse heterogeneity Spatio-Temporal Graph (STG) datasets show that SC-FSGL outperforms state-of-the-art methods.

---

## 论文详细总结（自动生成）

### 论文总结：受因果启发的动态时空图联邦学习 (SC-FSGL)

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**动态时空图联邦学习 (FSTG)** 中的数据异构性问题。
*   **背景**：时空图（如交通流、传感器网络）在空间结构和时间模式上具有高度的非平稳性和异构性。
*   **核心问题**：现有的联邦图学习方法通常假设所有特征在客户端之间是同等可迁移的。但在现实中，这种假设会导致：
    1.  **表示纠缠**：通用模式与客户端特有噪声混杂。
    2.  **特定干扰**：全局模型过拟合于局部非迁移信号。
    3.  **负迁移**：聚合后的模型在局部适配和全局泛化上表现均下降。
*   **动机**：受因果推理启发，区分“共享因果知识”（如普遍的交通规律）和“客户端特定知识”（如特定区域的道路特性），以打破负迁移的恶性循环。

#### 2. 方法论：核心思想与关键技术
论文提出了 **SC-FSGL** 框架，其核心思想是通过表示层面的干预来解耦可迁移的因果因素。

*   **条件分离模块 (Conditional Separation Module)**：
    *   利用**学习掩码 (Learnable Soft Mask)** 模拟因果干预中的 `do-operator`。
    *   通过公式 $\Phi(X) = (1 + LN(M)) \odot X$ 在潜在空间中衰减客户端特定变量的影响，从而提取出跨客户端稳定的不变特征。
*   **因果代码簿 (Causal Codebook)**：
    *   在服务器端维护一个全局共享的**原型矩阵 (Prototypes)**。
    *   客户端通过对比学习（Contrastive Learning）将局部提取的因果表示与代码簿中的正负样本对齐，确保不同客户端在语义空间的一致性。
*   **不变风险最小化 (IRM) 正则化**：
    *   引入 IRM 惩罚项，强制预测器在所有客户端环境下的梯度范数最小化，从而保证模型学习到的是跨环境的不变因果机制。
*   **损失函数**：总损失由预测损失 ($L_{mse}$)、对比对齐损失 ($L_{com}$) 和 IRM 正则项 ($L_{irm}$) 共同组成。

#### 3. 实验设计
*   **数据集**：使用了 5 个真实的交通流量数据集：**METRLA, PEMSD4, PEMSD7(M), PEMSD8, PEMSBAY**。每个数据集代表一个独立的客户端，以模拟强烈的时空异构性。
*   **Benchmark 与对比方法**：
    *   **通用联邦学习**：FedAvg, FedProx, Moon, FedRep。
    *   **图联邦学习**：FedStar。
    *   **时空预测模型**：GMAN, MegaCRN。
    *   **最新 FSTG 方法**：FUELS (2025)。
    *   **基准**：Local（仅本地训练，不进行联邦聚合）。
*   **评估指标**：MAE, RMSE, MAPE。

#### 4. 资源与算力
*   **硬件环境**：论文明确指出实验运行在 **2 台 NVIDIA 3090 GPU** 上。
*   **软件框架**：基于 PyTorch 实现。
*   **训练细节**：通信轮数设置了早停机制（Early Stopping），每轮局部训练仅执行 1 个 Epoch。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个数据集上进行了全方位的性能对比。
    *   针对 15、30、60 分钟三个预测步长进行了测试。
    *   **消融实验**：分别验证了“无代码簿”、“无分离模块”以及“两者均无”的情况，证明了各组件的有效性。
    *   **可视化分析**：包括 t-SNE 表示空间可视化、预测曲线拟合对比、MAE 收敛曲线分析。
*   **充分性评价**：实验设计较为全面，涵盖了从宏观性能到微观表示学习效果的验证。通过跨数据集的平均表现评估，增强了结果的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：SC-FSGL 在所有 5 个数据集和所有预测时间步长上均显著优于 SOTA 方法（如 FUELS 和 GMAN）。
*   **有效解耦**：t-SNE 可视化证明，该方法能清晰地将共享因果特征（Sc, Tc）与私有特定特征（So, To）分离。
*   **收敛稳定性**：相比于 FedRep 或 Moon，SC-FSGL 的 MAE 下降曲线更加平滑且最终误差更低，说明因果解耦有助于稳定联邦优化过程。
*   **通信效率**：通过仅交换轻量级的因果代码簿（约 8KB）而非全模型参数，极大地降低了通信开销。

#### 7. 优点（亮点）
*   **因果视角**：首次在动态时空图联邦学习中引入因果干预思想，解决了特征纠缠导致的负迁移问题。
*   **架构创新**：条件分离模块与因果代码簿的结合，既实现了知识共享，又保留了局部个性化。
*   **隐私与效率并重**：代码簿机制不仅保护了模型参数隐私，还实现了极高的通信压缩比。

#### 8. 不足与局限
*   **领域局限性**：实验主要集中在交通预测领域，尚未在环境监测、智能电网等其他时空图场景中验证通用性。
*   **超参数敏感性**：IRM 和对比学习的权重（$\alpha, \beta$）对结果有一定影响，实际部署时可能需要精细调优。
*   **动态演化建模**：虽然处理了动态图，但对于“因果关系本身随时间漂移”的长周期场景，论文未做深入探讨。

（完）
