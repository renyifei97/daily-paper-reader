---
title: "Forecasting with Guidance: Representation-Level Supervision for Time Series Forecasting"
title_zh: 带引导的预测：时间序列预测的表示级监督
authors: "Jiacheng Wang, Liang Fan, Baihua Li, Luyan Zhang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24262v1"
tags: ["query:urt-pf"]
score: 8.0
evidence: 基于深度学习和基础模型的时间序列预测
tldr: 针对当前时间序列预测模型因过度优化平均误差而忽略极端模式、导致预测过于平滑的问题，本文提出了一种名为ReGuider的通用插件方法。该方法利用预训练的时间序列大模型作为“语义导师”，在训练过程中将目标模型的中间层嵌入与大模型的丰富语义表示进行对齐。通过这种表示层级的监督，ReGuider能够引导编码器捕获更具表现力的时序动态。实验证明，该方法在多种架构和数据集上均能显著提升预测性能，具有极强的通用性和有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24262v1/fig-001.webp\", \"caption\": \"TABLE III: Results of Time-MoE foundation model—base, large, and ultra—as representation guiders.\", \"page\": 4, \"index\": 1, \"width\": 522, \"height\": 486}, {\"url\": \"assets/figures/arxiv/2603.24262v1/fig-002.webp\", \"caption\": \"TABLE II: Results of Euclidean distance (ED), KL divergence (KLD), and Cosine Similarity (Cos. Sim.) as optimization objectives for representation supervision.\", \"page\": 4, \"index\": 2, \"width\": 523, \"height\": 486}, {\"url\": \"assets/figures/arxiv/2603.24262v1/fig-003.webp\", \"caption\": \"Fig. 2: Visualisation of the iTransformer’s and guider’s representations.\", \"page\": 5, \"index\": 3, \"width\": 1022, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2603.24262v1/fig-004.webp\", \"caption\": \"TABLE I: Long term forecasting results with varying predict lengths T ∈ {96, 192, 336, 720}. The historical input length L is fixed at 96 for fair comparison. The best results are highlighted in bold.\", \"page\": 3, \"index\": 4, \"width\": 1068, \"height\": 941}, {\"url\": \"assets/figures/arxiv/2603.24262v1/fig-005.webp\", \"caption\": \"Fig. 1: Overall architecture of ReGuider, which consists of the base predictor Fθ(·) and the foundation model Gϕ(·), serving as a representation guide.\", \"page\": 2, \"index\": 5, \"width\": 529, \"height\": 297}]"
motivation: 传统的端到端误差损失训练容易导致模型忽略具有信息量的极端模式，从而产生过于平滑的预测和表现力不足的特征表示。
method: 提出ReGuider插件，通过将目标模型的中间嵌入与预训练时间序列大模型的语义嵌入进行对齐，实现表示层级的监督引导。
result: 在多种数据集和预测架构上的广泛实验表明，ReGuider一致地提升了预测精度，验证了其在捕捉显著动态方面的有效性。
conclusion: 通过引入预训练大模型的表示级指导，可以显著增强时序编码器的特征提取能力，是一种通用且高效的预测增强方案。
---

## 摘要
如今，时间序列预测主要通过使用基于误差的目标函数对深度学习架构进行端到端训练来完成。虽然这在最小化平均损失方面是有效的，但它会促使编码器丢弃具有信息量但极端的模式。这导致了平滑的预测结果，且其时间表示难以捕捉显著的动态变化。为了解决这一问题，我们提出了 ReGuider，这是一种可以无缝集成到任何预测架构中的插件式方法。ReGuider 利用预训练的时间序列基础模型作为语义教师。在训练过程中，输入序列由目标预测模型和预训练模型共同处理。我们并非直接使用预训练模型的输出，而是提取其包含丰富时间与语义信息的中间嵌入，并通过表示级监督将其与目标模型的编码器嵌入进行对齐。这种对齐过程使编码器能够学习到更具表现力的时间表示，从而提高下游预测的准确性。在不同数据集和架构上的广泛实验表明，我们的 ReGuider 能够持续提升预测性能，证实了其有效性和通用性。

## Abstract
Nowadays, time series forecasting is predominantly approached through the end-to-end training of deep learning architectures using error-based objectives. While this is effective at minimizing average loss, it encourages the encoder to discard informative yet extreme patterns. This results in smooth predictions and temporal representations that poorly capture salient dynamics. To address this issue, we propose ReGuider, a plug-in method that can be seamlessly integrated into any forecasting architecture. ReGuider leverages pretrained time series foundation models as semantic teachers. During training, the input sequence is processed together by the target forecasting model and the pretrained model. Rather than using the pretrained model's outputs directly, we extract its intermediate embeddings, which are rich in temporal and semantic information, and align them with the target model's encoder embeddings through representation-level supervision. This alignment process enables the encoder to learn more expressive temporal representations, thereby improving the accuracy of downstream forecasting. Extensive experimentation across diverse datasets and architectures demonstrates that our ReGuider consistently improves forecasting performance, confirming its effectiveness and versatility.

---

## 论文详细总结（自动生成）

这是一份关于论文《Forecasting with Guidance: Representation-Level Supervision for Time Series Forecasting》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前深度学习时间序列预测（TSF）模型主要依赖基于误差（如 MSE、MAE）的端到端训练。这种方式虽然能最小化平均损失，但会导致编码器倾向于丢弃具有信息量的极端模式，产生过于平滑的预测。
*   **研究背景**：这种现象被称为“表示坍缩（Representation Collapse）”，即模型生成的潜在表示在语义上是贫乏的，只能捕捉趋势而丢失了底层的生成动力学。
*   **研究动机**：作者认为提升预测性能的关键不在于设计更复杂的架构，而在于引入外部的语义监督。预训练的时间序列基础模型（TSFM）在海量数据上学习到了丰富的“通用时间词汇”，可以作为“导师”来引导特定任务模型的学习。

### 2. 论文提出的方法论：ReGuider
*   **核心思想**：提出一种名为 **ReGuider** 的插件式方法，通过将目标预测模型的中间层嵌入（Embeddings）与预训练基础模型的中间层嵌入进行对齐，实现表示层级的监督。
*   **关键技术细节**：
    *   **双路径架构**：输入序列 $X$ 同时输入目标预测模型 $F_\theta$ 和预训练基础模型 $G_\phi$。
    *   **表示提取**：不使用基础模型的预测头，而是提取其编码器的中间嵌入 $H_g$。同时提取目标模型编码器的嵌入 $H_f$。
    *   **非对称梯度流**：在训练过程中，基础模型 $G_\phi$ 的参数是被冻结的（Stop-gradient），作为稳定的语义锚点，防止表示漂移。
    *   **损失函数**：总体损失由预测损失和表示对齐损失组成：
        $L_{total} = L_{Pred}(Y, \hat{Y}) + \lambda \cdot sim(H_f, H_g)$
        其中 $sim$ 是距离度量函数，实验表明**欧几里得距离（Euclidean Distance）**效果最优。
*   **算法流程**：在训练阶段，通过最小化 $H_f$ 与 $H_g$ 的距离，强制目标模型学习基础模型所具备的丰富时间语义；在推理阶段，基础模型被移除，目标模型独立运行。

### 3. 实验设计
*   **数据集**：使用了 7 个主流 Benchmark 数据集，涵盖了电力（ETTh1/h2, ETTm1/m2）、天气（Weather）、用电量（ECL）和交通（Traffic）。
*   **Benchmark 与对比方法**：
    *   **基础预测器（Backbones）**：选择了四种具有代表性的模型：iTransformer（基于 Transformer）、PatchTST（基于 Patch）、DLinear（基于线性层）和 TimeMixer（多尺度混合）。
    *   **基础模型（Guider）**：主要使用 Time-MoE（Base, Large, Ultra 版本）。
*   **对比逻辑**：对比了上述四种模型在“原始状态”与“集成 ReGuider 插件后”的性能表现。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练时长。
*   **效率分析**：作者提到 ReGuider 仅在训练阶段调用基础模型，虽然会增加一定的训练开销（显存和计算量），但在**推理阶段完全不增加任何成本**，因为基础模型在部署时不需要参与。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 7 个数据集上进行了长程预测实验（预测长度 $T \in \{96, 192, 336, 720\}$）。
    *   针对 4 种不同的模型架构验证了通用性。
    *   **消融实验**：对比了三种距离函数（ED, KLD, Cosine Similarity）的影响。
    *   **模型规模实验**：测试了不同参数量（Base, Large, Ultra）的基础模型作为引导者的效果。
    *   **可视化分析**：通过 t-SNE 降维展示了受引导后的表示空间更加紧凑且具辨别力。
*   **评价**：实验设计较为充分且客观，通过跨架构、跨数据集的对比验证了该插件的普适性。

### 6. 主要结论与发现
*   **性能提升**：ReGuider 在所有测试的架构上均实现了性能提升，平均精度提高超过 **5%**。
*   **高维优势**：在变量极多（如 Traffic 数据集，862 个变量）的复杂场景下，提升尤为显著，说明基础模型的通用语义有助于捕捉复杂的变量间耦合。
*   **长程稳定性**：随着预测长度增加，ReGuider 能够作为“语义锚点”，有效缓解模型在长程预测中的潜在表示漂移问题。
*   **对齐策略**：点对点的欧几里得距离比余弦相似度或 KL 散度更能提供稳定的监督信号。

### 7. 优点（亮点）
*   **模型无关性（Model-agnostic）**：作为一个插件，它可以无缝集成到任何现有的 TSF 架构中，无需修改原模型结构。
*   **推理零成本**：通过知识迁移的方式增强模型，在实际部署时保持了原有的轻量化特性。
*   **解决本质痛点**：针对 MSE 损失导致的“预测平滑”问题，从表示学习的角度给出了有效的解决方案。

### 8. 不足与局限
*   **训练开销**：虽然推理无成本，但训练时需要加载巨大的基础模型（如 Time-MoE Ultra），这对于显存受限的设备是一个挑战。
*   **依赖基础模型质量**：该方法的效果高度依赖于所选基础模型的预训练质量。如果基础模型在特定领域（如极低频数据）表现不佳，可能会引入负面引导。
*   **超参数敏感性**：损失函数中的权衡因子 $\lambda$ 可能需要针对不同数据集进行精细调优，论文未详细讨论其敏感性。

（完）
