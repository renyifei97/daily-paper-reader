---
title: Channel-wise Retrieval for Multivariate Time Series Forecasting
title_zh: 多变量时间序列预测的逐通道检索
authors: "Junhyeok Kang, Jun Seo, Soyeon Park, Sangjun Han, Seohui Bae, Hyeokjun Choe, Soonyoung Lee"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05543v1"
tags: ["query:urt-pf"]
score: 8.5
evidence: 多变量时间序列预测的通道检索
tldr: 针对多变量时间序列预测中固定回看窗口难以捕捉长程依赖且现有检索方法忽略变量间异质性的问题，本文提出CRAFT框架。该框架通过为每个通道独立检索历史片段，并结合时域稀疏关系图剪枝与频域谱相似度排序，有效捕捉了不同变量的独特周期性。实验证明CRAFT在七个基准数据集上均优于现有模型，兼具高精度与推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05543v1/fig-001.webp\", \"caption\": \"Table 1. Performance comparison of multivariate time series forecasting with state-of-the-art baselines on the public benchmark datasets. We set forecasting horizon H ∈ {96, 192, 336, 720} for all methods and report the average. The results are mostly taken from previous studies [7].\", \"page\": 4, \"index\": 1, \"width\": 1046, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2604.05543v1/fig-002.webp\", \"caption\": \"Fig. 1. Comparison of retrieval strategies. In channelagnostic retrieval, all variables use the same retrieved multivariate segment. In channel-wise retrieval (ours), each variable independently retrieves its own references, respecting heterogeneity across variables.\", \"page\": 1, \"index\": 2, \"width\": 508, \"height\": 251}]"
motivation: 现有的检索增强预测方法采用通道无关策略，忽略了多变量时间序列中不同变量之间存在的异质性和独特的周期性特征。
method: 提出CRAFT框架，采用两阶段检索：先在时域构建稀疏关系图进行候选剪枝，再在频域利用谱相似度进行排序以强调主导周期成分。
result: 在七个公开基准数据集上的实验结果表明，CRAFT的预测准确率超越了当前最先进的基准模型。
conclusion: 通道级检索结合频域相似度匹配能更有效地捕捉长程依赖，是提升多变量时间序列预测性能的有效途径。
---

## 摘要
多变量时间序列预测通常由于固定的回顾窗口而难以捕捉长期依赖关系。检索增强预测通过从记忆中检索历史片段来解决这一问题，但现有方法依赖于通道无关策略，即对所有变量应用相同的参考。这忽略了变量间的异质性，即不同通道表现出截然不同的周期性和频谱特征。我们提出了 CRAFT（逐通道检索增强预测），这是一个为每个通道独立执行检索的新型框架。为了确保效率，CRAFT 采用了两阶段流水线：在时域构建的稀疏关系图用于剪枝无关候选，而频域中的频谱相似度用于对参考进行排序，在抑制噪声的同时强调主要的周期性成分。在七个公开基准数据集上的实验表明，CRAFT 优于最先进的预测基准模型，在具备实用推理效率的同时实现了卓越的准确性。

## Abstract
Multivariate time series forecasting often struggles to capture long-range dependencies due to fixed lookback windows. Retrieval-augmented forecasting addresses this by retrieving historical segments from memory, but existing approaches rely on a channel-agnostic strategy that applies the same references to all variables. This neglects inter-variable heterogeneity, where different channels exhibit distinct periodicities and spectral profiles. We propose CRAFT (Channel-wise retrieval-augmented forecasting), a novel framework that performs retrieval independently for each channel. To ensure efficiency, CRAFT adopts a two-stage pipeline: a sparse relation graph constructed in the time domain prunes irrelevant candidates, and spectral similarity in the frequency domain ranks references, emphasizing dominant periodic components while suppressing noise. Experiments on seven public benchmarks demonstrate that CRAFT outperforms state-of-the-art forecasting baselines, achieving superior accuracy with practical inference efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《Channel-wise Retrieval for Multivariate Time Series Forecasting》（CRAFT）的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统多变量时间序列预测模型受限于固定的“回顾窗口”（Lookback Window），难以捕捉远距离的历史依赖。
*   **研究背景**：现有的检索增强（RAG）预测方法多采用“通道无关”（Channel-agnostic）策略，即所有变量共享同一个检索到的历史片段。
*   **动机**：多变量数据具有显著的**异质性**，不同通道（变量）的周期性、季节性和动态特性各异。强制所有变量使用相同的参考时间线会引入噪声并降低预测精度。

### 2. 方法论：CRAFT 框架
CRAFT 提出了**逐通道检索（Channel-wise Retrieval）**范式，核心思想是让每个变量独立寻找最相关的历史参考。为解决计算复杂度问题，设计了两阶段流水线：

*   **知识库构建与稀疏关系图（第一阶段 - 时域剪枝）**：
    *   计算变量间的余弦相似度，为每个变量构建一个**稀疏关系图**。
    *   每个变量仅与其最相似的 $M$ 个邻居变量建立连接，将搜索空间从全量内存缩减到局部候选集。
*   **频谱相似度检索（第二阶段 - 频域排序）**：
    *   利用快速傅里叶变换（FFT）将时域序列转为频域表示，并仅保留前 $F$ 个低频分量。
    *   在推理时，通过**归一化复数内积**计算查询序列与候选参考之间的频谱相似度。
    *   这种设计能有效突出主导周期特征并抑制高频噪声。
*   **预测集成**：
    *   将检索到的 Top-R 参考值通过线性映射投影到预测区间。
    *   最终预测结果由基础预测器（如 MLP）的直接输出与检索分支的加权修正值相加得到：$\hat{Y} = \hat{Y}_{direct} + \alpha \cdot \hat{Y}_{retrieval}$。

### 3. 实验设计
*   **数据集**：涵盖 7 个主流公开基准数据集：ETTh1, ETTh2, ETTm1, ETTm2, Electricity (ECL), Traffic, Weather。
*   **预测任务**：长程预测，预测步长 $H \in \{96, 192, 336, 720\}$。
*   **对比方法（Baselines）**：包括 RAFT (ICML 2025)、TimeMixer、PatchTST、TimesNet、MICN、DLinear、Stationary、Autoformer 等 8 种最先进模型。
*   **评估指标**：均方误差 (MSE) 和 平均绝对误差 (MAE)。

### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA RTX 3090 GPU** 上完成。
*   **软件/框架**：PyTorch 2.0.1，使用 Adam 优化器。
*   **效率表现**：文中提到推理效率极高，每批次（Batch size 32）耗时约 0.03 秒，相当于每秒可处理超过 1000 个实例。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集上进行了全方位的性能对比，涵盖了不同的预测长度。
*   **消融与分析**：
    *   针对候选邻居数量 $M$ 进行了敏感度分析（图 3）。
    *   提供了定性的可视化分析（图 4），展示了检索到的片段与真实值的匹配程度。
*   **客观性**：实验遵循了时间序列预测的标准评估协议，对比基准包含了 2021-2025 年间的最前沿模型，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能领先**：CRAFT 在所有 7 个基准数据集上的平均 MSE 和 MAE 均优于现有模型，证明了逐通道检索的优越性。
*   **效率可行性**：通过稀疏图和频域截断，CRAFT 将复杂度从 $O(C^2 \cdot |M|)$ 降低到 $O(C \cdot M \cdot F)$，实现了线性扩展，适合实时应用。
*   **异质性重要性**：实验证实，允许不同变量检索各自的参考资料能显著减少预测误差。

### 7. 优点
*   **创新的检索范式**：首次在 RAG 预测中引入逐通道检索，解决了多变量异质性问题。
*   **高效的两阶段设计**：巧妙结合了时域的稀疏性（减少搜索范围）和频域的鲁棒性（减少计算维度并去噪）。
*   **即插即用**：作为一种加性修正机制，CRAFT 可以轻松集成到任何现有的预测架构中，无需改变其核心结构。

### 8. 不足与局限
*   **超参数敏感性**：模型依赖于多个超参数（如邻居数 $M$、频率分量数 $F$、权重 $\alpha$），在不同数据集上可能需要精细调优。
*   **内存依赖**：虽然检索效率高，但仍需维护一个外部历史记忆库，对于极大规模或流式数据，内存管理可能成为挑战。
*   **变量间关联的简化**：虽然考虑了变量异质性，但通过简单的余弦相似度构建稀疏图可能无法捕捉变量间复杂的非线性动态演化关系。

（完）
