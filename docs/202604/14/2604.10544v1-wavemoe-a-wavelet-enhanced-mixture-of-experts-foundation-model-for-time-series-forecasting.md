---
title: "WaveMoE: A Wavelet-Enhanced Mixture-of-Experts Foundation Model for Time Series Forecasting"
title_zh: WaveMoE：一种用于时间序列预测的小波增强混合专家基础模型
authors: "Shunyu Wu, Jiawei Huang, Weibin Feng, Boxin Li, Xiao Zhang, Erli Meng, Dan Li, Jian Lou, See-Kiong Ng"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10544v1"
tags: ["query:urt-pf"]
score: 8.0
evidence: 用于通用时间序列预测的小波增强基础模型
tldr: 本研究针对时间序列基础模型在捕捉复杂频率模式（如周期性和局部高频动态）方面的不足，提出了 WaveMoE。该模型采用双路径架构，将时域标记与对齐的小波标记共同处理，并通过共享的混合专家（MoE）路由机制实现高效的容量扩展与专家分工。WaveMoE 在 16 个基准数据集上展现了卓越的预测潜力，为结合频域表征与大规模基础模型提供了新视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10544v1/fig-001.webp\", \"caption\": \"Table 3: Forecasting performance (MSE and MAE) of WaveMoE compared with baseline models on additional benchmark datasets. The best results for each dataset are bolded.\", \"page\": 10, \"index\": 1, \"width\": 822, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.10544v1/fig-002.webp\", \"caption\": \"Table 2: Forecasting performance (MSE and MAE) of WaveMoE compared with baseline models on 16 benchmark datasets. The best results for each dataset are bolded.\", \"page\": 5, \"index\": 2, \"width\": 818, \"height\": 410}]"
motivation: 旨在解决现有时间序列基础模型难以充分建模现实数据中复杂的周期性及局部高频特征的问题。
method: 提出一种双路径 MoE 架构，同步处理时域和对齐的小波域标记，并利用共享路由机制协调专家分工。
result: 在 16 个不同领域的基准数据集上，WaveMoE 通过整合小波域语料库显著提升了时间序列的预测效果。
conclusion: 将显式频域表示集成到可扩展的 MoE 基础模型中，是进一步提升通用时间序列预测性能的有效方向。
---

## 摘要
时间序列基础模型 (TSFMs) 最近通过在多样化的时间序列数据上进行大规模预训练，在通用预测领域取得了显著成功。作为这一进展的补充，引入频域信息在增强复杂时间模式（如现实世界时间序列中普遍存在的周期性和局部高频动态）建模方面表现出了极具前景的性能。为了推进这一方向，我们提出了一种将显式频域表示集成到可扩展基础模型中的新视角，并介绍了 WaveMoE，这是一种用于时间序列预测的小波增强混合专家基础模型。WaveMoE 采用双路径架构，共同处理沿统一时间轴对齐的时间序列 token 和小波 token，并通过共享的专家路由机制对它们进行协调，从而在高效扩展模型容量的同时实现一致的专家专业化。在 16 个多样化基准数据集上的初步实验结果表明，通过引入小波域语料库，WaveMoE 具有进一步提升预测性能的潜力。

## Abstract
Time series foundation models (TSFMs) have recently achieved remarkable success in universal forecasting by leveraging large-scale pretraining on diverse time series data. Complementing this progress, incorporating frequency-domain information yields promising performance in enhancing the modeling of complex temporal patterns, such as periodicity and localized high-frequency dynamics, which are prevalent in real-world time series. To advance this direction, we propose a new perspective that integrates explicit frequency-domain representations into scalable foundation models, and introduce WaveMoE, a wavelet-enhanced mixture-of-experts foundation model for time series forecasting. WaveMoE adopts a dual-path architecture that jointly processes time series tokens and wavelet tokens aligned along a unified temporal axis, and coordinates them through a shared expert routing mechanism that enables consistent expert specialization while efficiently scaling model capacity. Preliminary experimental results on 16 diverse benchmark datasets indicate that WaveMoE has the potential to further improve forecasting performance by incorporating wavelet-domain corpora.

---

## 论文详细总结（自动生成）

这是一份关于论文《WaveMoE: A Wavelet-Enhanced Mixture-of-Experts Foundation Model for Time Series Forecasting》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在时间序列基础模型（TSFMs）领域，现有的研究主要集中在原始时域语料库的规模扩展上。论文探讨了一个关键的科学问题：**在预训练语料中引入频域（如小波域）信息，是否能进一步提升基础模型的预测性能？**
*   **研究背景**：现实世界的时间序列包含复杂的周期性、局部振荡和多尺度动态。虽然小规模模型已证明频域信息的有效性，但在大规模、可扩展的基础模型（尤其是混合专家网络 MoE 架构）中，如何高效集成显式的频域表示仍是一个挑战。

### 2. 方法论：WaveMoE 架构
WaveMoE 采用了一种**双路径（Dual-Path）混合专家架构**，核心思想是同步建模时域和频域特征：
*   **双路径分词（Tokenization）**：
    *   **时域路径**：将输入序列切分为固定长度的补丁（Patches）。
    *   **小波域路径**：利用离散小波变换（DWT）将序列分解为多尺度近似和细节系数，并将其分组为与时域补丁在时间轴上完全对齐的小波 token。
*   **稀疏注意力机制**：在两条路径中分别使用 Transformer 风格的自注意力，并通过 Top-k 评分仅激活信息量最大的 token，以降低长序列的计算开销。
*   **统一 MoE 路由（Unified Routing）**：
    *   这是模型的核心创新。它不为两个领域设置独立路由，而是使用**共享路由网络**。
    *   将同一时间位置的时域和小波 token 拼接后输入 MLP 门控模块，决定专家分配。
    *   每个专家内部包含两个独立的分支（FFN），分别处理时域和小波信息，确保在共享路由逻辑的同时保留领域特定的感官偏置。
*   **联合监督学习**：输出层设有独立预测头，分别预测原始数值和小波系数，通过联合损失函数进行训练。

### 3. 实验设计
*   **数据集**：涵盖了 16 个多样化的基准数据集，包括能源（ETT, Ercot）、金融（Exchange Rate）、交通（Traffic）、天气（Weather）、零售（M5, M4）以及医疗和 IoT 数据。
*   **Benchmark 与对比方法**：
    *   **主要对比对象**：Time-MoE（具有相似 MoE 架构但仅限时域）。
    *   **其他基准 TSFMs**：Timer、Chronos、Sundial。
*   **实验设置**：固定上下文长度为 512，预测长度为 96。使用均方误差（MSE）和平均绝对误差（MAE）作为评价指标。

### 4. 资源与算力
*   **预训练数据**：基于 Time-300B 语料库，经过领域平衡、质量过滤和 IoT 数据增强，最终保留约 **980 亿个时间步**。
*   **模型规模**：总参数量为 2.26 亿（226M），推理时激活参数量为 1 亿（100M）。
*   **算力细节**：文中提到使用了 AdamW 优化器，训练步数为 100,000 步，Batch Size 为 128。**但论文未明确说明具体的 GPU 型号、数量及总训练时长。**

### 5. 实验数量与充分性
*   **实验规模**：论文在正文和附录中总共测试了超过 30 个数据集（正文 16 个，附录补充了大量其他场景），涵盖了极广的领域。
*   **充分性评价**：
    *   **客观性**：通过与多种最先进的基础模型对比，证明了其在 MSE/MAE 上的领先地位。
    *   **局限性**：虽然数据集覆盖广泛，但缺乏针对不同小波基函数或分解层数的消融实验，且目前仅针对单变量预测场景（Channel-independent）。

### 6. 主要结论与发现
*   **性能提升**：WaveMoE 在 16 个主要数据集中有 14 个取得了最佳 MSE，11 个取得了最佳 MAE。
*   **频域价值**：实验证明，通过小波变换引入显式的频域感官偏置，能显著增强模型捕捉局部高频波动和复杂周期性的能力。
*   **可视化发现**：相比于 Chronos 或 Timer 等模型，WaveMoE 在预测波峰、波谷以及处理突发趋势转折时具有更高的保真度，减少了预测轨迹过度平滑的问题。

### 7. 优点与亮点
*   **架构创新**：巧妙地将小波变换与 MoE 架构结合，通过“共享路由+独立分支”的设计，解决了多域信息在基础模型中协同扩展的难题。
*   **时间对齐**：通过 DWT 保持小波 token 与时域 token 的时间对齐，使得模型能够维持一致的时间索引逻辑。
*   **泛化能力**：在跨领域的零样本/微调预测中表现出极强的鲁棒性。

### 8. 不足与局限
*   **多变量建模**：目前采用通道独立（Channel-independent）策略，尚未充分探索变量间的相关性。
*   **计算复杂度**：虽然使用了稀疏注意力和 MoE，但双路径设计相比单路径模型在数据预处理（DWT）和特征维度上仍增加了额外开销。
*   **超参数敏感性**：小波基的选择（如 bior2.2）和分解层数对不同特征的数据可能有较大影响，论文尚未给出自适应选择的方案。

（完）
