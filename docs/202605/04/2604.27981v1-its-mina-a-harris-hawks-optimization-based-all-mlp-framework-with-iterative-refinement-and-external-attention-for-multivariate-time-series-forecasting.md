---
title: "ITS-Mina: A Harris Hawks Optimization-Based All-MLP Framework with Iterative Refinement and External Attention for Multivariate Time Series Forecasting"
title_zh: ITS-Mina：一种基于哈里斯鹰优化、结合迭代细化与外部注意力的全 MLP 多变量时间序列预测框架
authors: "Pourya Zamanvaziri, Amirhossein Sadr, Aida Pakniyat, Dara Rahmati"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27981v1"
tags: ["query:urt-pf"]
score: 8.0
evidence: 用于交通规划的多变量时间序列预测
tldr: 针对多元时间序列预测中Transformer模型计算开销大的问题，本文提出ITS-Mina框架。该框架采用全MLP架构，通过迭代细化机制增强时间表示，引入外部注意力机制捕捉跨样本全局依赖，并利用哈里斯鹰优化算法自动调整丢弃率。实验证明，该模型在六个基准数据集上达到了最先进的预测性能，兼顾了计算效率与预测精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27981v1/fig-001.webp\", \"caption\": \"Table 2: MSE comparison across different horizons and datasets. The best result in each row is in bold and the second best is underlined.\", \"page\": 15, \"index\": 1, \"width\": 973, \"height\": 754}, {\"url\": \"assets/figures/arxiv/2604.27981v1/fig-002.webp\", \"caption\": \"Figure 1: Overall architecture of ITS-Mina. The input is instance-normalized, refined by N applications of a shared depth-M residual mixer stack, augmented by external attention, and mapped to the forecast horizon with inverse normalization.\", \"page\": 7, \"index\": 2, \"width\": 972, \"height\": 547}, {\"url\": \"assets/figures/arxiv/2604.27981v1/fig-003.webp\", \"caption\": \"Figure 2: External attention in ITS-Mina (notation aligned with Section 5.1). Rows of H ∈ RL×C attend over S shared slots via E; the mixture is mapped by V and added residually to yield Z.\", \"page\": 10, \"index\": 3, \"width\": 679, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.27981v1/fig-004.webp\", \"caption\": \"Table 3: MAE comparison across different horizons and datasets. The best result in each row is in bold and the second best is underlined.\", \"page\": 16, \"index\": 4, \"width\": 973, \"height\": 750}]"
motivation: 旨在解决现有Transformer模型在多元时间序列预测中计算复杂度高，且传统MLP模型在捕捉复杂依赖关系方面存在局限的问题。
method: 结合了共享参数的残差混合器迭代细化机制、基于可学习记忆单元的外部注意力模块，以及用于自动超参数调优的哈里斯鹰优化算法。
result: 在六个主流基准数据集上的实验结果显示，ITS-Mina在多个预测周期下均优于或等同于十一种基准模型。
conclusion: ITS-Mina证明了通过改进MLP架构并引入启发式优化算法，可以在保持低计算成本的同时实现卓越的时间序列预测效果。
---

## 摘要
多变量时间序列预测在金融分析、能源管理和交通规划等众多现实应用中发挥着关键作用。虽然基于 Transformer 的架构在此任务中广受欢迎，但最近的研究表明，更简单的基于 MLP 的模型能够以显著降低的计算成本实现具有竞争力或更优越的性能。在本文中，我们提出了 ITS-Mina，这是一种用于多变量时间序列预测的新型全 MLP 框架，它集成了三项关键创新：(1) 迭代细化机制，通过重复应用共享参数的残差混合器堆栈来逐步增强时间表示，在不增加不同参数数量的情况下有效加深了模型的计算能力；(2) 外部注意力模块，用可学习的记忆单元取代传统的自注意力，以线性计算复杂度捕捉跨样本的全局依赖关系；(3) 哈里斯鹰优化（HHO）算法，用于自动调整 Dropout 率，实现针对每个数据集量身定制的自适应正则化。在六个广泛使用的基准数据集上进行的广泛实验表明，与 11 个基准模型相比，ITS-Mina 在多个预测步长下均实现了最先进或极具竞争力的性能。

## Abstract
Multivariate time series forecasting plays a pivotal role in numerous real-world applications, including financial analysis, energy management, and traffic planning. While Transformer-based architectures have gained popularity for this task, recent studies reveal that simpler MLP-based models can achieve competitive or superior performance with significantly reduced computational cost. In this paper, we propose ITS-Mina, a novel all-MLP framework for multivariate time series forecasting that integrates three key innovations: (1) an iterative refinement mechanism that progressively enhances temporal representations by repeatedly applying a shared-parameter residual mixer stack, effectively deepening the model's computational capacity without multiplying the number of distinct parameters; (2) an external attention module that replaces traditional self-attention with learnable memory units, capturing cross-sample global dependencies at linear computational complexity; and (3) a Harris Hawks Optimization (HHO) algorithm for automatic dropout rate tuning, enabling adaptive regularization tailored to each dataset. Extensive experiments on six widely-used benchmark datasets demonstrate that ITS-Mina achieves state-of-the-art or highly competitive performance compared to eleven baseline models across multiple forecasting horizons.

---

## 论文详细总结（自动生成）

这是一份关于论文《ITS-Mina: A Harris Hawks Optimization-Based All-MLP Framework with Iterative Refinement and External Attention for Multivariate Time Series Forecasting》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
多变量时间序列预测（MTSF）在金融、能源和交通等领域至关重要。尽管基于 Transformer 的架构因其捕捉长程依赖的能力而成为主流，但近期研究（如 DLinear）发现，简单的 MLP 模型在保持低计算成本的同时，性能往往能媲美甚至超越复杂的 Transformer。
然而，现有的 MLP 模型存在三个主要局限：
*   **缺乏迭代能力**：通常采用单次前向传播，无法通过迭代逐步细化复杂模式的预测。
*   **缺乏全局上下文**：主要依赖局部特征交互，难以捕捉训练数据分布中的跨样本全局相关性。
*   **超参数调节困难**：关键超参数（如 Dropout 率）通常依赖耗时的手动网格搜索。

**研究动机**：设计一个全 MLP 框架，在不显著增加参数量的前提下，通过迭代机制、外部注意力和启发式优化算法来克服上述缺陷。

### 2. 论文提出的方法论
论文提出了 **ITS-Mina** 框架，其核心由三个创新组件构成：

*   **共享参数的迭代细化机制（Iterative Refinement）**：
    *   模型包含一个深度为 $M$ 的残差混合器堆栈（Mixer Stack），该堆栈在单次推理中被重复调用 $N$ 次。
    *   通过**权值共享（Weight Tying）**，模型在增加计算深度和感受野的同时，保持了与单层堆栈相同的参数规模，有效防止了在小数据集上的过拟合。
*   **外部注意力模块（External Attention）**：
    *   放弃了计算复杂度为 $O(L^2)$ 的自注意力，引入两个可学习的记忆矩阵（Memory Units/Slots）。
    *   该模块通过将输入投影到共享的外部记忆空间，以线性复杂度 $O(L)$ 捕捉跨样本的全局依赖，并起到隐式正则化的作用。
*   **基于哈里斯鹰优化（HHO）的自动调优**：
    *   将 Dropout 率的确定视为连续空间优化问题。
    *   利用 HHO 算法模拟鹰群捕食的探索与开发策略，自动搜索针对特定数据集的最优 Dropout 率，取代了传统的手动搜索。

### 3. 实验设计
*   **数据集**：使用了 6 个主流基准数据集：Traffic（交通）、Electricity（电力）以及 ETT 系列（ETTh1, ETTh2, ETTm1, ETTm2）。
*   **预测周期（Horizons）**：涵盖了 96, 192, 336, 720 四个时间步长。
*   **对比方法（Baselines）**：共 11 个模型，分为三类：
    *   **Transformer 类**：Informer, Autoformer, FEDformer, Crossformer, iTransformer。
    *   **MLP 类**：DLinear, CI-TSMixer, PatchMixer。
    *   **其他**：PatchTST（基于 Patch 的 Transformer）, TimesNet, MICN。
*   **评估指标**：均方误差（MSE）和平均绝对误差（MAE）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA RTX 3090 GPU** 上进行。
*   **软件/框架**：使用 Adam 优化器，结合 Optuna 框架进行结构超参数搜索（如隐藏层维度、学习率等），随后使用 HHO 优化 Dropout。
*   **训练细节**：最大训练轮数为 100 Epochs，带有早停机制（Patience=5）。文中未明确给出总的训练时长，但强调了其 MLP 架构相比 Transformer 具有更高的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个数据集、4 个预测周期上进行了详尽测试，共产生数十组对比数据。
*   **充分性**：实验覆盖了高维（Traffic）和低维（ETT）数据，以及不同采样频率（小时、15分钟）。
*   **客观性**：作者使用了 Optuna 进行自动超参数搜索，减少了人工调优带来的偏差；对比基准均采用了原始论文报告的最优结果或官方实现，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：ITS-Mina 在绝大多数数据集和预测周期组合中达到了 **SOTA（最先进）** 性能。特别是在 MSE 指标上，ITS-Mina 在所有测试设置中均排名第一。
*   **高维数据优势**：在 Traffic（862特征）和 Electricity（321特征）等复杂数据集上，ITS-Mina 的领先优势尤为明显。
*   **迭代与注意力的协同**：实验证明，迭代细化增强了时间表示的深度，而外部注意力则成功补足了 MLP 在全局建模上的短板。

### 7. 优点（亮点）
*   **参数效率极高**：通过权值共享的迭代机制，实现了“深层”的效果却只占用“浅层”的参数空间。
*   **线性复杂度**：外部注意力的引入解决了长序列预测中的计算瓶颈。
*   **自动化程度高**：集成 HHO 算法进行正则化参数自动搜索，提升了模型的易用性和鲁棒性。
*   **架构简洁**：回归全 MLP 设计，证明了在合理的机制增强下，简单架构依然具有极强的生命力。

### 8. 不足与局限
*   **计算开销权衡**：虽然参数量没增加，但迭代 $N$ 次意味着推理时的计算量（FLOPs）会随 $N$ 线性增长，论文未详细讨论推理延迟的增加。
*   **HHO 搜索成本**：HHO 优化过程需要多次训练模型以评估适应度（Fitness），这在超大规模数据集上可能会显著增加预训练阶段的时间成本。
*   **缺乏自适应停止**：目前的迭代次数 $N$ 是固定的，若能根据输入样本的复杂度动态调整迭代次数（Adaptive Halting），模型将更具灵活性。

（完）
