---
title: Training-Free Probabilistic Time-Series Forecasting with Conformal Seasonal Pools
authors: Valery Manokhin
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03789v1"
tags: ["query:urt-pf"]
score: 8.5
evidence: 交通数据集上的概率时间序列预测
tldr: 本文提出符合性季节性池（CSP），一种无需训练的概率时间序列预测方法。CSP通过结合季节性朴素预测、同季节经验样本和残差样本，在六个基准数据集上显著优于深度学习模型DeepNPTS。该方法在提升精度的同时，运行速度快500倍以上，且在覆盖率等关键指标上表现更稳健，证明了无需训练的基准在评估非参数预测模型时的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03789v1/fig-001.webp\", \"caption\": \"Figure 4: Accuracy vs. runtime. Lower mean CRPS rank is better; the x-axis is log-scaled wall time. CSP-Adaptive and CSP-Fixed sit in the upper-left training-free zone; DeepNPTS is dominated on rank and isolated by two orders of magnitude on cost while also failing the coverage target.\", \"page\": 11, \"index\": 1, \"width\": 867, \"height\": 470}, {\"url\": \"assets/figures/arxiv/2605.03789v1/fig-002.webp\", \"caption\": \"Table 7: Audited method wall time. Dataset download/loading and final summary writing are excluded.\", \"page\": 15, \"index\": 2, \"width\": 891, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2605.03789v1/fig-003.webp\", \"caption\": \"Figure 1: Per-window rank distribution (380 forecast windows). Greener bars indicate more top-rank finishes; the rightmost dark-red band indicates rank 5–6. CSP variants concentrate in the top three ranks on both metrics.\", \"page\": 7, \"index\": 3, \"width\": 927, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2605.03789v1/fig-004.webp\", \"caption\": \"Table 1: Accuracy and runtime summary across the 6 datasets and 380 forecast records per method. Lower ranks are better; coverage targets 0.95.\", \"page\": 7, \"index\": 4, \"width\": 935, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2605.03789v1/fig-005.webp\", \"caption\": \"Table 5: Absolute-score summary across the 6 datasets. CRPS is reported as a within-dataset ratio to the cross-method median to control for the wide range of dataset scales. MQL is the normalized mean quantile loss. Coverage targets 0.95.\", \"page\": 14, \"index\": 5, \"width\": 938, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2605.03789v1/fig-006.webp\", \"caption\": \"Table 6: Per-window paired Wilcoxon signed-rank tests (n ≈ 380 paired records per comparison). Bold p-values reach p < 0.05. CRPS and MQL are normalized within each (dataset, series, window) by the median across methods.\", \"page\": 14, \"index\": 6, \"width\": 882, \"height\": 529}, {\"url\": \"assets/figures/arxiv/2605.03789v1/fig-007.webp\", \"caption\": \"Table 2: Per-window rank-finish counts (out of 380 forecast windows). Each window is ranked 1 (best) to 6 (worst) within itself; columns count the windows landing in each rank band. Rows sum to 380.\", \"page\": 8, \"index\": 7, \"width\": 912, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2605.03789v1/fig-008.webp\", \"caption\": \"Figure 3: Per-dataset mean coverage, sorted by gap. CSP-Adaptive (green) covers near or above 0.95 on every dataset; DeepNPTS (red) undercovers everywhere; gap labels above each pair show the per-dataset CSP-DeepNPTS coverage advantage in absolute coverage units.\", \"page\": 9, \"index\": 8, \"width\": 871, \"height\": 334}]"
motivation: 针对深度非参数预测模型在安全关键应用中存在的覆盖率严重不足、计算成本高及校准失效等风险。
method: 提出CSP方法，通过将季节性朴素预测与同季节的经验观测值及带符号的残差样本相结合，实现无需参数学习的概率预测。
result: 实验表明CSP在各项预测指标上均显著优于DeepNPTS，且在CPU上的运行速度提升了500倍以上。
conclusion: 研究强调无需训练的符合性采样器应成为评估深度非参数预测模型时强制性的基准测试标准。
---

## Abstract
We propose Conformal Seasonal Pools (CSP), a training-free probabilistic time-series forecaster that mixes same-season empirical draws with signed residual draws around a seasonal naive forecast. In an audited rolling-origin benchmark on the six time-series datasets where DeepNPTS was originally evaluated (electricity, exchange_rate, solar_energy, taxi, traffic, wikipedia), CSP-Adaptive significantly outperforms DeepNPTS on every metric we report -- CRPS (per-window paired Wilcoxon $p \approx 4 \times 10^{-10}$), normalized mean quantile loss ($p \approx 7 \times 10^{-10}$), and empirical 95% coverage ($p \approx 8 \times 10^{-45}$, mean 0.89 vs 0.66) -- while running over 500x faster on CPU. Coverage is the most decision-critical of these: a 0.95 nominal interval that contains the truth in only ~66% of cases fails the basic calibration desideratum and would not survive deployment in safety- or decision-critical settings. The failure mode is also more severe than aggregate coverage suggests: in the worst 10% of windows, DeepNPTS's prediction interval covers none of the H forecast horizons -- the entire multi-step trajectory misses the truth at every step simultaneously. This poses serious risk in safety- and decision-critical applications such as healthcare, finance, energy operations, and autonomous systems, where prediction intervals that systematically miss the truth across the entire planning horizon translate directly into misclassified patients, regulatory capital failures, grid imbalances, and safety-case violations. CSP achieves all of this with no learned parameters and no training. We argue training-free conformal samplers should be mandatory baselines when evaluating learned non-parametric forecasters.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Conformal Seasonal Pools (CSP)** 的新型概率时间序列预测方法。以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在概率预测领域，深度学习模型（如 DeepNPTS）虽然流行，但往往面临计算成本高、缺乏透明度以及在安全关键任务中**校准失效（Calibration Failure）**的问题。
*   **研究动机**：作者发现许多复杂的神经非参数预测器在 95% 的名义覆盖率下，实际覆盖率远低于预期（仅约 66%），这在医疗、金融和自动驾驶等领域是不可接受的。论文旨在探索：在不进行任何神经网络训练的情况下，仅依靠经验分布和符合性预测（Conformal Prediction）技术，预测性能能达到什么水平。

### 2. 方法论：Conformal Seasonal Pools (CSP)
*   **核心思想**：CSP 是一种**无需训练（Training-Free）**的采样器，它通过混合两种来源的样本来构建预测分布：
    1.  **季节性经验池（Seasonal Empirical Pool）**：从历史数据中抽取处于相同季节性相位的观测值。
    2.  **符合性残差池（Conformal Residual Pool）**：在季节性朴素预测（Seasonal Naive Forecast）的基础上，叠加从校准窗口中提取的带符号残差。
*   **关键技术细节**：
    *   **混合机制**：对于每个预测步 $h$，样本 $S_h$ 由上述两个池按权重 $w_h$ 混合而成。
    *   **自适应规则（CSP-Adaptive）**：当检测到数据缺乏季节性（周期 $m \le 1$）或季节性样本过少时，自动降低季节性池的权重，退化为纯残差采样器。
    *   **符合性校准**：利用最近的历史数据作为校准集，提取带符号残差以捕捉局部的水平偏移和不对称性。

### 3. 实验设计
*   **数据集**：使用了 DeepNPTS 原始评估中的 6 个标准数据集：`electricity`（电力）、`exchange_rate`（汇率）、`solar_energy`（太阳能）、`taxi`（出租车）、`traffic`（交通）和 `wikipedia`（维基百科）。
*   **Benchmark 与对比方法**：
    *   **外部对比**：DeepNPTS（主要的神经非参数基准）、NPTS、SeasonalNPTS。
    *   **内部对比**：CSP-Adaptive、CSP-Fixed（固定权重 0.5）、AdaptiveWindowMCI（无季节性结构的快速基准）。
*   **评估指标**：CRPS（连续概率排位分数）、归一化平均分位数损失（MQL）、经验 95% 覆盖率、区间宽度和运行时间。

### 4. 资源与算力
*   **算力使用**：实验主要在 **CPU** 上运行。
*   **训练时长对比**：
    *   **CSP 变体**：处理全部 380 个预测记录仅需约 **0.27 分钟**。
    *   **DeepNPTS**：完成相同任务需要 **153.1 分钟**。
    *   **结论**：DeepNPTS 在 CPU 上的运行速度比 CSP 慢了 **567 倍**。论文未提及 GPU 训练的具体型号，但强调了 CSP 无需训练的特性使其在任何硬件上都具有压倒性的速度优势。

### 5. 实验数量与充分性
*   **实验规模**：共生成了 380 个预测记录，涵盖 6 个数据集，每个数据集包含 10 条序列，采用滚动起始（Rolling-Origin）协议。
*   **充分性与公平性**：
    *   **公平性**：使用了与 DeepNPTS 论文完全相同的评估套件和数据集，确保了“主场”对比的公正性。
    *   **统计显著性**：通过配对 Wilcoxon 符号秩检验（Paired Wilcoxon Test）对每组窗口进行显著性分析，结果显示 CSP 在 CRPS 和覆盖率上的优势具有极高的统计显著性（$p$ 值极小）。

### 6. 主要结论与发现
*   **性能超越**：无需训练的 CSP-Adaptive 在所有 6 个数据集上的 CRPS 和 MQL 指标均显著优于 DeepNPTS。
*   **校准可靠性**：DeepNPTS 存在严重的校准失效，名义 95% 的区间实际仅覆盖 66% 的真实值；而 CSP 达到了约 89% 的覆盖率，且在最差的 10% 窗口中表现远比 DeepNPTS 稳健。
*   **效率极高**：CSP 在保持高精度的同时，计算成本降低了两个数量级以上。
*   **基准意义**：作者认为，无需训练的符合性采样器应当成为评估所有非参数预测模型的**强制性基准**。

### 7. 优点
*   **简单且透明**：算法逻辑清晰，易于审计和部署，没有黑盒神经网络的不可解释性。
*   **鲁棒性强**：通过结合季节性先验和符合性残差，有效解决了纯经验采样在水平偏移和季节性缺失时的失效问题。
*   **即时预测**：无需昂贵的重训练过程，非常适合需要快速响应的生产环境。

### 8. 不足与局限
*   **独立性假设**：CSP 在每个预测步独立采样，未显式建模多步预测轨迹之间的相干性（Coherence），可能不适用于需要联合分布决策的场景。
*   **理论保证**：虽然借鉴了符合性预测的思想，但在非平稳时间序列下，CSP 并不提供严格的有限样本覆盖保证（这也是目前时间序列符合性预测的共同挑战）。
*   **超参数固定**：实验中使用的校准比例、衰减率等超参数是固定的，未针对特定数据集进行精细调优，可能仍有提升空间。

（完）
