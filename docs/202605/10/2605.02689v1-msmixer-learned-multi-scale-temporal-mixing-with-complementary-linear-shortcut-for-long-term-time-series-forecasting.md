---
title: "MSMixer: Learned Multi-Scale Temporal Mixing with Complementary Linear Shortcut for Long-Term Time Series Forecasting"
title_zh: MSMixer：用于长期时间序列预测的带互补线性捷径的学习型多尺度时间混合模型
authors: Ahmed Cherif
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02689v1"
tags: ["query:urt-pf"]
score: 8.0
evidence: 用于长期时间序列预测的多尺度时间混合
tldr: "本研究针对长期时间序列预测中轻量级MLP模型难以捕捉多尺度模式的问题，提出了MSMixer。该模型采用通道独立的并行多尺度分支（1x, 4x, 16x）和可学习的Softmax门控机制，并结合DLinear残差连接以增强趋势和季节性建模。实验表明，MSMixer在保持极低参数量（112K）和线性复杂度的同时，在多个基准数据集上显著优于DLinear等轻量级模型，且性能可媲美复杂的Transformer模型。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02689v1/fig-001.webp\", \"caption\": \"Table 2 Forecasting results (MSE/MAE) on four ETT benchmarks (look-back T=336, standard chronological splits). Results for MSMixer, DLinear, and NLinear are averaged over 3 seeds. †denotes results from published papers. Bold: best MSE per row; underline: second best.\", \"page\": 11, \"index\": 1, \"width\": 733, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2605.02689v1/fig-002.webp\", \"caption\": \"Fig. 5 Training and validation loss convergence for MSMixer on ETTh1 across all four prediction horizons. Early stopping halts training within 4–8 epochs for most configurations.\", \"page\": 15, \"index\": 2, \"width\": 723, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2605.02689v1/fig-003.webp\", \"caption\": \"Fig. 1 Architecture of MSMixer. After RevIN normalisation, the channel-independent time series is downsampled at multiple temporal resolutions. Each resolution is independently processed by a lightweight MLP block. The multi-resolution outputs are merged via a learnable softmax gate and fused with a DLinear shortcut that provides global trend and seasonality context from the full lookback window.\", \"page\": 7, \"index\": 3, \"width\": 709, \"height\": 531}, {\"url\": \"assets/figures/arxiv/2605.02689v1/fig-004.webp\", \"caption\": \"Fig. 3 Ablation study results (ETTh1, H = 96). MSE comparison across architectural variants with error bars (±1 std).\", \"page\": 12, \"index\": 4, \"width\": 639, \"height\": 351}, {\"url\": \"assets/figures/arxiv/2605.02689v1/fig-005.webp\", \"caption\": \"Table 1 Parameter count breakdown of MSMixer (T = 336, H = 96, d = 64, S = {1, 4, 16}, N = 7).\", \"page\": 9, \"index\": 5, \"width\": 775, \"height\": 292}]"
motivation: 现有的轻量级MLP模型通常在单一时间分辨率下运行，难以同时捕捉快速波动、中程周期和宏观趋势。
method: 提出一种包含三个并行多尺度分支、可学习门控权重以及DLinear互补快捷连接的通道独立MLP架构。
result: 在ETT基准测试中，MSMixer以仅112K的参数量实现了0.357的最低平均MSE，优于DLinear和NLinear等主流轻量级模型。
conclusion: MSMixer证明了通过多尺度时间混合和互补线性结构，轻量级模型可以在极低计算开销下实现卓越的预测性能。
---

## 摘要
长期时间序列预测要求模型能够从固定的回顾窗口中同时捕捉快速振荡、中程周期性和缓慢演变的宏观趋势。现有的轻量级基于 MLP 的模型通常在单一时间分辨率上运行，限制了它们显式建模多尺度模式的能力。我们提出了 MSMixer，这是一种通道独立的多尺度 MLP 架构，通过三项互补创新解决了这一局限性：(i) 具有 {1x, 4x, 16x} 下采样因子的三个并行尺度分支，配备独立的 MLP 块；(ii) 一个可学习的 softmax 门，用于动态加权分支输出；(iii) 一个 DLinear 互补捷径，提供全窗口的趋势和季节性上下文。MSMixer 在预测长度 H=96 时仅包含 112K 个参数，并以 O(T) 的复杂度运行。在四个 ETT 基准测试上，采用标准的时间顺序划分和三个随机种子进行评估，MSMixer 在轻量级模型中实现了最低的平均 MSE (0.357)，优于 DLinear (0.386, -7.4%) 和 NLinear (0.365, -2.1%)，在 16 种配置中胜出 12 种。与文献中的五个基于 Transformer 的基准模型相比，MSMixer 在 16 种配置中的 9 种实现了最佳或次佳的 MSE，同时使用的参数比 PatchTST 少 5 倍。消融实验和敏感性分析证实了多尺度分支和 DLinear 捷径的互补贡献。

## Abstract
Long-term time series forecasting requires models that simultaneously capture rapid oscillations, medium-range periodicities, and slowly evolving macro-trends from a fixed look-back window. Existing lightweight MLP-based models typically operate on a single temporal resolution, limiting their ability to explicitly model patterns at multiple scales. We propose MSMixer, a channel-independent multi-scale MLP architecture that addresses this limitation through three complementary innovations: (i) three parallel scale branches at down-sample factors {1x, 4x, 16x} with independent MLP blocks, (ii) a learnable softmax gate that dynamically weighs branch outputs, and (iii) a DLinear complementary shortcut that provides full-window trend and seasonality context. MSMixer contains only 112K parameters at H=96 and runs at O(T) complexity. Evaluated on four ETT benchmarks with standard chronological splits and three random seeds, MSMixer achieves the lowest average MSE (0.357) among lightweight models, outperforming DLinear (0.386, -7.4%) and NLinear (0.365, -2.1%), winning 12 of 16 configurations. Against five Transformer-based baselines from the literature, MSMixer achieves best or second-best MSE in 9 of 16 configurations while using 5x fewer parameters than PatchTST. Ablation and sensitivity analyses confirm the complementary contributions of the multi-scale branches and the DLinear shortcut.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MSMixer** 的轻量级长时时间序列预测模型。以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
长期时间序列预测（LTSF）的挑战在于信号往往是多种成分的叠加：包括快速波动的噪声、中程周期性模式（如日循环）以及缓慢演变的宏观趋势（如周或月趋势）。
*   **现有问题**：现有的轻量级 MLP（多层感知机）模型（如 DLinear）通常在单一时间分辨率下运行，难以同时显式建模这些多尺度模式。
*   **研究目标**：在保持极低参数量和线性计算复杂度的前提下，通过多尺度表示增强模型捕捉不同频率特征的能力。

### 2. 方法论：MSMixer 架构
MSMixer 采用通道独立（Channel-Independent）的设计，核心思想是通过并行分支处理不同分辨率的时间序列，并结合线性捷径保留全局上下文。

*   **多尺度分支设计**：
    *   输入序列经过 **RevIN**（可逆实例归一化）处理以应对分布偏移。
    *   设置三个并行分支，下采样因子分别为 **{1×, 4×, 16×}**。
    *   使用 **平均池化（Average Pooling）** 进行下采样。1× 分支保留全分辨率细节，4× 捕捉中程周期，16× 提取粗粒度趋势。
    *   每个分支配备独立的双层 MLP 块处理各自的分辨率。
*   **可学习的 Softmax 门控（Scale Gate）**：
    *   引入可学习参数 $\gamma$，通过 Softmax 函数动态计算三个分支的权重 $w_s$，实现分支输出的加权融合。
*   **DLinear 互补捷径（Shortcut）**：
    *   由于下采样分支会丢失全局窗口信息，模型引入了一个 DLinear 模块作为捷径。它将序列分解为趋势和季节性成分，直接对原始长度 $T$ 进行线性投影。
*   **融合与输出**：
    *   最终预测结果由多尺度分支的加权和与 DLinear 捷径的输出进行融合（融合比例 $\alpha$ 也是可学习的），最后通过 RevIN 逆变换恢复原始量纲。

### 3. 实验设计
*   **数据集**：使用了四个标准的 ETT（电力变压器温度）基准数据集：ETTh1、ETTh2（每小时采样）和 ETTm1、ETTm2（每 15 分钟采样）。
*   **预测配置**：回顾窗口 $T=336$，预测长度 $H \in \{96, 192, 336, 720\}$。
*   **对比方法**：
    *   **轻量级模型**：DLinear, NLinear。
    *   **Transformer 类模型**：PatchTST, iTransformer, TimesNet, FEDformer, Autoformer。
*   **评估指标**：均方误差（MSE）和平均绝对误差（MAE）。

### 4. 资源与算力
*   **硬件环境**：论文明确提到实验是在 **CPU** 上进行的（由于模型极其轻量）。
*   **训练时长**：在 ETT 数据集上，16 种配置的总训练时间在 **6 小时以内**。
*   **参数量**：在 $H=96$ 时仅有 **112K** 参数，远低于 PatchTST 等模型（通常 >500K）。
*   **复杂度**：推理复杂度为 $O(T)$，与序列长度呈线性关系。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个数据集、4 种预测长度，共 16 种标准配置。
*   **重复性**：所有自研实验均使用了 **3 个随机种子**（42, 2023, 7）并报告平均值和标准差，增强了结果的可信度。
*   **消融实验**：针对 DLinear 捷径、不同尺度组合、RevIN 模块等进行了详细的消融研究。
*   **敏感性分析**：探讨了尺度数量 $|S|$ 和回顾窗口长度 $T$ 对性能的影响。
*   **客观性**：直接对比了复现的线性模型，并引用了 Transformer 类模型的公认基准结果，实验设计较为公平。

### 6. 主要结论与发现
*   **性能领先**：MSMixer 在轻量级模型中表现最优，平均 MSE 为 0.357，比 DLinear 提升了 7.4%。
*   **媲美复杂模型**：在 16 种配置中，MSMixer 有 9 种达到了最佳或次佳水平。在 ETTm1 ($H=336$) 和 ETTm2 ($H=720$) 等长序列任务上，甚至击败了参数量大 5 倍的 PatchTST。
*   **多尺度价值**：在 15 分钟采样的 ETTm 数据集上提升最明显，说明多尺度设计在更高采样频率（更宽时间带宽）的数据中优势更大。
*   **门控权重发现**：学习到的权重显示 1× 分支通常最重要，但 4× 和 16× 分支贡献了约 60% 的决策权重，证明了多尺度的必要性。

### 7. 优点与亮点
*   **极简且高效**：通过简单的平均池化和 MLP 实现了多尺度建模，避免了复杂的树状结构或频域变换。
*   **互补性设计**：巧妙地结合了 MLP 的局部特征提取能力和 DLinear 的全局线性趋势捕捉能力。
*   **可解释性**：通过可学习的门控权重 $\gamma$ 和融合因子 $\alpha$，可以直观观察模型在不同数据集上对尺度的偏好。
*   **低门槛**：对算力要求极低，适合资源受限的工业应用场景。

### 8. 不足与局限
*   **数据集覆盖有限**：实验仅限于 ETT 系列电力数据集，缺乏在交通、天气、金融等更多样化领域验证其通用性。
*   **通道独立性限制**：模型不考虑变量间的相关性（Cross-variate dependencies），在强相关多元序列任务中可能不如 iTransformer 等模型。
*   **尺度选择固定**：{1, 4, 16} 是人为设定的固定值，对于周期性不符合 4 的幂次的数据集，可能不是最优选择。
*   **基准对比偏差**：部分 Transformer 模型的结果来自文献引用而非同步复现，可能存在超参数或预处理差异带来的微小偏差。

（完）
