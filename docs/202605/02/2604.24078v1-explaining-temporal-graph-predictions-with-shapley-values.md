---
title: Explaining Temporal Graph Predictions With Shapley Values
title_zh: 利用 Shapley 值解释时序图预测
authors: "Lea-Marie Sussek, Stefan Heindorf"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24078v1"
tags: ["query:urt-pf"]
score: 8.0
evidence: 解释结合了空间和时间信息的时空图神经网络预测
tldr: 本研究针对时序图神经网络（TGNN）预测机制不透明的问题，提出了两种基于博弈论的局部解释方法。首先是事件级解释器，利用KernelSHAP估算单个时序事件的贡献；其次是特征级解释器，通过Owen值将事件贡献进一步分解为特征依赖关系。该方法不仅在多个基准测试中超越了现有技术，还能有效识别模型实现中的逻辑缺陷（如TGAT的时间戳提取问题），显著提升了TGNN的可信度与可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24078v1/fig-001.webp\", \"caption\": \"Table 1: Comparison of AUCs for different explainers. Left block: events removed (zeroed out). Right block: events replaced by mean event or mean feature value. Bold is the best performing explainer and the second best is underlined. For all metrics, higher means better.\", \"page\": 6, \"index\": 1, \"width\": 1048, \"height\": 570}, {\"url\": \"assets/figures/arxiv/2604.24078v1/fig-002.webp\", \"caption\": \"Figure 1: Comparison of the explanations generated by the different explainers. The connection between A and B is predicted and the prediction needs to be explained. TGNN-Explainer returns a subset of important connected events. TempME assigns each event an importance score. The thickness of the event indicates its importance. The Event Explainer (ours) quantifies the average contribution to the prediction. Purple and green means positive or negative influence, respectively. The thicker the line the more dominant is the influence.\", \"page\": 4, \"index\": 2, \"width\": 998, \"height\": 345}]"
motivation: 针对时序图神经网络预测过程缺乏透明度、可能存在偏差或错误的问题，探索其内部决策机制。
method: 提出了基于Shapley值的事件级解释器和基于Owen值的特征级解释器，实现了对TGNN预测的层次化分解。
result: 实验证明该方法在多个数据集上优于现有最先进解释器，并成功揭示了TGAT模型在时间戳处理上的实现缺陷。
conclusion: 该研究通过引入博弈论解释框架，为理解和诊断时序图模型的复杂行为提供了强有力的工具。
---

## 摘要
时序图神经网络 (TGNNs) 近年来因其结合空间和时间信息而表现出的卓越预测性能而日益受到关注。然而，这些模型如何利用信息进行预测在很大程度上尚未得到探索，这可能导致模型存在缺陷或偏见。本研究介绍了两种基于 Shapley 值和 Owen 值的、用于 TGNN 局部解释的新型模型无关解释器。第一种方法是事件级（边级）Shapley 解释器，它应用 KernelSHAP 算法来估计单个时序事件的贡献分数，为模型行为提供可解释的描述。第二种是特征级 Shapley 解释器，它通过将事件级 Shapley 值分解为 Owen 值来扩展该框架，从而揭示事件及其特征的层次依赖关系。这些解释器在不同的指标和数据集上优于 SOTA 解释器。此外，特征解释器揭示了一个常用 TGAT 实现中对实际时间戳的错误提取，有助于进一步理解在极稀疏解释下性能下降的原因。

## Abstract
Temporal Graph Neural Networks (TGNNs) have become increasingly popular in recent years due to their superior predictive performance by combining both spatial and temporal information. However, how these models utilize the information to make predictions is rather unexplored, leading to potentially faulty or biased models. This work introduces two novel model-agnostic explainers for local explanations of TGNNs based on Shapley and Owen values. The first method, an event-level (edge-level) Shapley explainer, applies the KernelSHAP algorithm to estimate contribution scores for individual temporal events, providing interpretable descriptions for model behavior. The second, a feature-level Shapley explainer, extends this framework by decomposing event-level Shapley values into Owen values, and thereby uncovers hierarchical dependencies of the event and its features. The explainers outperform SOTA explainers on different metrics and datasets. Additionally, the Feature Explainer reveals a faulty extraction of actual timestamps of a commonly used TGAT implementation, helping to further understand performance drops on very sparse explanations.

---

## 论文详细总结（自动生成）

这篇论文针对时序图神经网络（TGNN）的黑盒特性，提出了基于博弈论的解释框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：时序图神经网络（TGNN）虽然在处理动态图数据（如社交网络、金融交易）方面表现卓越，但其决策过程极度不透明。
*   **研究动机**：
    *   现有的静态图解释器无法捕捉时间动态。
    *   现有的时序图解释器（如 TempME, TGNN-Explainer）通常依赖于额外的可训练辅助模型，导致计算缓慢、对超参数敏感，且无法区分贡献的正负向。
    *   需要一种模型无关（Model-agnostic）、无需额外训练且能提供细粒度（事件级和特征级）解释的方法。

### 2. 方法论
论文提出了两种互补的解释器：
*   **事件级解释器 (Event Explainer)**：
    *   **核心思想**：将解释过程建模为合作博弈，每个时序事件（边）视为一名“玩家”。
    *   **技术细节**：利用 **KernelSHAP** 算法估算每个事件对预测结果的 Shapley 值。为了在移除事件时不破坏图的连通性，引入了“平均事件（Average Event）”的概念进行掩码处理，即用特征均值和平均时间间隔代替被移除的事件。
*   **特征级解释器 (Feature Explainer)**：
    *   **核心思想**：利用 **Owen 值**（Shapley 值的层次化扩展）将单个事件的贡献进一步分解。
    *   **技术细节**：将事件分解为时间戳、源节点特征和事件属性特征。采用“两步法”近似计算：先计算事件的 Shapley 值，再通过蒙特卡洛采样和 KernelSHAP 将其分配给内部特征。这种方法保证了层次加和一致性（即特征贡献之和等于事件贡献）。

### 3. 实验设计
*   **数据集**：
    *   **真实场景**：MOOC（学生互动）、Wikipedia（编辑行为）、Reddit（社交互动）。
    *   **合成场景**：Generated Dataset（专门用于测试时间效应的受控数据集）。
*   **对比基准 (Benchmarks)**：
    *   **TGNN-Explainer**：基于搜索和强化学习的解释器。
    *   **TempME**：基于时序基元（Motif）发现的解释器。
*   **评估指标**：Fidelity（保真度）、Fidelity (Logit)、Deviation（偏差）和 GEF（图解释忠实度）。

### 4. 资源与算力
*   **硬件环境**：实验在高性能计算（HPC）集群上完成，使用了 **NVIDIA A100 (40GB) GPU** 和 80GB RAM。
*   **计算细节**：KernelSHAP 采样量采用了 SHAP 库的默认配置，蒙特卡洛采样设置为 550 次，以平衡运行时间和解释方差。

### 5. 实验数量与充分性
*   **实验规模**：针对每个稀疏度阈值（0 到 1 之间）生成了 **200 组解释**并取平均值。
*   **充分性评价**：实验覆盖了回归和分类任务，对比了两种不同的事件移除策略（直接移除 vs. 均值替换），并涵盖了多种 TGNN 架构（如 TGAT, TGN, CAWN）。实验设计客观、对比方法具有代表性，能够充分验证所提方法的优越性。

### 6. 主要结论与发现
*   **性能领先**：在多数指标和数据集上，该方法（尤其是特征解释器）的 AUC 表现优于 SOTA 方法。
*   **诊断价值**：特征解释器成功揭示了常用 **TGAT 实现中的一个逻辑缺陷**——模型在处理无邻居节点时，会意外地通过注意力机制泄露绝对时间戳信息，从而导致“伪高精度”。
*   **效率权衡**：事件解释器推理速度快（约 1.8 秒），而特征解释器由于双层采样较慢（约 50 秒/事件），更适合针对性分析。

### 7. 优点
*   **模型无关性**：不需要 TGNN 模型可微，也不需要修改模型架构或进行额外训练。
*   **层次化解释**：通过 Owen 值实现了从事件到特征的无缝分解，提供了极高的解释深度。
*   **诊断能力**：不仅能解释预测，还能作为调试工具发现模型实现中的潜在 Bug。

### 8. 不足与局限
*   **计算开销**：特征级解释器的采样计算量较大，在大规模图上进行全量解释可能存在性能瓶颈。
*   **独立性假设**：Shapley 值计算假设输入特征（事件）可以独立修改，但在复杂的图结构中，事件之间往往存在强耦合，这可能在某些极端情况下影响解释的严谨性。

（完）
