---
title: Collision-Free Velocity Scheduling for Multi-Agent Systems on Predefined Routes via Inexact-Projection ADMM
title_zh: 基于非精确投影 ADMM 的预定义路线上多智能体系统无碰撞速度调度
authors: "Seungyeop Lee, Jong-Han Kim"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21913v1"
tags: ["query:urt-c"]
score: 8.0
evidence: 预定路径上多智能体系统的速度调度
tldr: 针对预定义路径下的多智能体系统，本文提出一种基于非精确投影交替方向乘子法（ADMM）的速度调度方案。该方法通过优化航点通过时间来避免碰撞，利用可微轨迹模型捕捉动态特性，并采用梯度下降进行碰撞修正，有效避免了复杂的整数规划。实验证明该方法在多种拥堵场景下均能高效生成无碰撞且时间最优的调度方案，显著提升了任务完成效率。
selection_source: fresh_fetch
motivation: 旨在解决路径受限的多智能体运输系统中，如何在不改变空间路径的前提下，仅通过优化速度调度来确保避障安全与系统效率。
method: 提出一种结合可微轨迹代理模型与非精确投影ADMM的算法，利用梯度下降处理非凸碰撞约束，避免了引入复杂的整数排序变量。
result: 在随机交叉、瓶颈及图网络等多种复杂场景下的数值实验表明，该方法能快速生成可行调度，且在瓶颈场景中优于传统的分层基准方法。
conclusion: 该研究为受限路径下的多智能体协同提供了一种高效的数值优化框架，证明了基于梯度的速度调度在处理高密度交通流中的有效性。
---

## 摘要
在结构化多智能体运输系统中，智能体通常必须遵循预定义的路线，这使得空间重路由变得不可取或不可能。本文通过优化航点通过时间，同时保持每个智能体分配的航点顺序和名义路线分配，来解决受路线约束的多智能体协调问题。一个可微的代理轨迹模型将航点时间映射到平滑的位置剖面，并捕捉一阶跟踪滞后，从而能够通过在跨越任务周期的密集时间网格上评估基于距离的惩罚项来编码两两安全性。由此产生的非线性和非凸速度调度问题使用一种非精确投影交替方向乘子法 (ADMM) 算法求解，该算法结合了结构化时间更新与基于梯度的碰撞修正步骤，并避免了显式的整数排序变量。在随机交叉、瓶颈和基于图的网络场景中进行的数值实验表明，所提出的方法在各种拥堵水平下都能计算出可行且高效的时间表，并且在测试的瓶颈案例中，其任务完成时间比具有代表性的分层基准方法更短。

## Abstract
In structured multi-agent transportation systems, agents often must follow predefined routes, making spatial rerouting undesirable or impossible. This paper addresses route-constrained multi-agent coordination by optimizing waypoint passage times while preserving each agent's assigned waypoint order and nominal route assignment. A differentiable surrogate trajectory model maps waypoint timings to smooth position profiles and captures first-order tracking lag, enabling pairwise safety to be encoded through distance-based penalties evaluated on a dense temporal grid spanning the mission horizon. The resulting nonlinear and nonconvex velocity-scheduling problem is solved using an inexact-projection Alternating Direction Method of Multipliers (ADMM) algorithm that combines structured timing updates with gradient-based collision-correction steps and avoids explicit integer sequencing variables. Numerical experiments on random-crossing, bottleneck, and graph-based network scenarios show that the proposed method computes feasible and time-efficient schedules across a range of congestion levels and yields shorter mission completion times than a representative hierarchical baseline in the tested bottleneck cases.

---

## 论文详细总结（自动生成）

### 论文总结：基于非精确投影 ADMM 的预定义路线上多智能体系统无碰撞速度调度

#### 1. 核心问题与研究动机
在城市空中交通（UAM）、自动化仓库物流和走廊机器人作业等场景中，智能体通常必须遵循预定义的结构化路线。由于基础设施或操作限制，智能体无法进行空间上的重路由（即不能左右绕行），只能通过调整沿路径的**运动时机（速度调度）**来避免碰撞。
*   **现有挑战**：传统的反应式方法（如 RVO）易陷入死锁；全局规划方法（如基于冲突的搜索或混合整数规划 MIP）在处理高密度交通时，由于离散决策变量的组合爆炸，计算扩展性较差。
*   **研究目标**：开发一种无需显式整数排序变量、能够处理非凸碰撞约束、且具有良好扩展性的连续时间速度调度框架。

#### 2. 方法论
论文提出了一种基于**非精确投影交替方向乘子法（ADMM）**的优化框架：
*   **决策变量**：将每个智能体经过各航点的时间（Waypoint Passage Times）作为优化变量。
*   **可微代理轨迹模型**：为了解决分段恒速模型在航点处速度不连续的问题，引入了基于 **Sigmoid 函数**的平滑过渡模型。该模型不仅可微，还能通过引入时间偏差和形状参数来捕捉实际控制系统的**一阶跟踪滞后**特性。
*   **优化目标**：最小化所有非固定到达时间智能体的总任务完成时间。
*   **约束处理**：
    *   **线性约束**：包括出发时间、速度限制（转化为航点间的时间差约束）。
    *   **非凸约束**：两两智能体间的安全距离。
*   **算法流程（ADMM）**：
    1.  **t-更新**：求解一个稀疏线性系统，更新航点时间。
    2.  **x-更新**：将时间差投影到速度限制定义的凸集上。
    3.  **z-更新（核心创新）**：针对非凸的碰撞约束，不进行精确投影，而是采用**非精确投影（Inexact Projection）**。利用基于梯度的 Polyak 型步长和**自适应动量机制**来修正碰撞冲突，从而避开显式的整数排序。

#### 3. 实验设计
论文在多种具有挑战性的合成场景中验证了算法：
*   **实验场景**：
    1.  **随机交叉（Random Crossing）**：包括同向（追赶避障）和对向（迎头避障）两种配置。
    2.  **瓶颈场景（Bottleneck）**：所有智能体必须通过一段狭窄的共享走廊。
    3.  **图网络场景（Graph-based Network）**：模拟 UAM 或物流网络，智能体在由 Delaunay 三角剖分生成的有向图路网上行驶。
*   **基准方法（Benchmark）**：对比了代表性的分层时间调度方法——**MIP-SOCP（混合整数规划-二阶锥规划）**框架。
*   **评价指标**：收敛迭代次数、任务完成时间开销（相对于最小行驶时间的百分比）、成功率（SR）。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 CPU/GPU 型号、内存等）。
*   算法实现使用了 Python，源代码已在 GitHub 开源。由于算法核心是求解稀疏线性系统和梯度计算，其计算开销主要集中在矩阵分解（仅需一次）和迭代更新上，通常可在普通 CPU 上实时或准实时运行。

#### 5. 实验数量与充分性
*   **实验规模**：针对不同场景，分别设置了智能体数量 $K \in \{3, 7, 11\}$ 和占用密度 $\phi \in \{1\%, 5\%, 10\%\}$ 的多组实验。
*   **充分性**：在对向随机交叉和图网络场景中，各进行了 **100 次蒙特卡洛模拟**。
*   **客观性**：通过归一化占用密度（Occupancy Density）来公平对比不同智能体数量下的拥堵程度。实验覆盖了从稀疏到极高密度的环境，验证了算法在物理可行边界附近的表现。

#### 6. 主要结论与发现
*   **高效性**：该方法在所有测试场景中均能生成可行且高效的速度剖面，收敛速度快（通常在 400 次迭代内）。
*   **优于基准**：在瓶颈场景中，该方法生成的任务完成时间比 MIP-SOCP 基准更短。在高密度（$\phi=10\%$）下，基准方法因无法处理复杂的排序逻辑而失效，而本算法仍能找到可行解。
*   **鲁棒性**：在图网络中，只要安全距离设定在几何可行范围内（即不违反路网本身的拓扑限制），算法成功率接近 100%。

#### 7. 优点
*   **无需先验优先级**：算法自动通过数值优化确定通过顺序，避免了人工或启发式分配优先级的繁琐。
*   **平滑性与物理一致性**：代理轨迹模型考虑了动力学滞后，使得生成的调度方案更易于被底层控制器跟踪。
*   **扩展性好**：通过 ADMM 分解，将复杂的非凸避障问题转化为简单的梯度修正，避免了整数规划的指数级复杂度。

#### 8. 不足与局限
*   **局部最优风险**：作为一种基于梯度的非凸优化方法，理论上存在陷入局部最优（死锁或无法收敛到可行解）的风险，尽管实验中通过自适应动量缓解了这一问题。
*   **动力学模型简化**：目前主要针对一阶滞后模型，对于具有高阶非线性动力学（如具有显著惯性的重型无人机）的系统，可能需要更复杂的代理模型。
*   **离线性质**：目前主要展示的是离线调度结果，虽然计算速度快，但尚未在论文中深入讨论大规模在线重规划（Replanning）的实时表现。

（完）
